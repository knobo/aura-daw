# SDD ledger — plan: docs/superpowers/plans/2026-08-28-plan-v2-players.md

Spec: docs/superpowers/specs/2026-08-26-plan-v-players-design.md (read; binding authority)
Worktree: /home/knobo/prog/dav/.worktrees/plan-v2-players
Branch: feat/plan-v2-players — PR #121 (draft)
Branch base: 6fe1112 (origin/main at claim time)
Human-readable progress mirror: docs/superpowers/plans/2026-08-28-plan-v2-players-PROGRESS.md (committed)

## Pre-flight scan

### Cross-task rows (pairs sharing a file or an interface)

| Tasks | Shared | Produces → consumes | Finding |
|---|---|---|---|
| 1 ↔ 6 | `audio/mod.rs` | both add one `pub mod` line | clean — sequential, different lines |
| 2 ↔ 5 | `audio/types.rs` | T2 adds `Store::players`/`Project::players`; T5 adds four functions over them | clean |
| 2 ↔ 12 | `audio/project.rs` | T2 carries `players` through `from_store`/`open_project`; T12 calls the migration in `open_project` | clean — T12 appends after T2's line |
| 4 ↔ 5 | raw players and send lanes | T4: raw `MixNode` has no sends; T5: `derive_send_slots_with_players` skips `!chain_applies()` | clean — the two agree by construction; a divergence would put a knob in a lane no graph reads |
| 6 ↔ 7 | `ClockTable` | T6 produces the table; T7 puts it on `RtGraph`/`GraphTables` | clean |
| 7 ↔ 8 | `engine.rs` clock sizing, `control/mod.rs` | T7 hardcodes `n_clocks = 2` + `SCENE_CLOCK = 1`; T8 replaces both with `1 + players.len() + scenes.len()` and a `scene_clocks` map | clean — T8 explicitly deletes T7's constant; T7's text says so |
| 8 ↔ 9 | clock index allocation | T8 assigns scenes `1 + players.len() + i`; T9 assigns players `1 + i` | clean — disjoint ranges, and T8's formula already reserves the player range before T9 fills it |
| 8 ↔ 12 | `midi/launch.rs` fire path | T8 collapses the origin arms onto `fire_scene`; T12 adds `LaunchTarget::Player` routed to `player_fire` | clean — between T8 and T12 a `Clip` target still fires a scene clock, which is the pre-migration behaviour preserved |
| 7 ↔ 9 | `audio/offline.rs` | T7 gives the bounce a 1-clock table; T9 adds the V-15 regression test | clean |
| 9 ↔ 10 | `engine.rs` player row loop | T9 writes the loop with `_ => Vec::new()`; T10 adds the `MidiClip` arm | **row shape differs** — T9's arm pushes `RtTrack::clips(slot, clips)` at the loop tail, T10's arm pushes and `continue`s. Ruling below. |
| 9 ↔ 11 | `player_fire` | T9 already passes `looping` from the trigger mode | **T11 is thinner than written** — ruling below |
| 3 ↔ 9 | `op.rs` / commands | T3 produces the ops; T9's `player_add`/`player_remove` commands commit them | clean |
| 9 ↔ 13 | command surface | T9 produces `player_fire`/`player_stop`/`player_add`/`players_get`; T13 consumes them | clean |
| 9/10 ↔ 14 | perf harness | T14 measures what T9/T10 added | clean |

### Per-task internal-agreement rows

| Task | Finding |
|---|---|
| 1 | clean — the test module's `use super::*` reaches `InsertSlot`, imported at module level by the impl |
| 2 | clean |
| 3 | **fixture does not exist** — tests call `test_session()`; `session.rs`'s test module has `session_with_one_track(id)`. Ruling below. `EngineEffect { rebuild }` verified to exist (`session.rs:575`). |
| 4 | clean — `test_track`, `InsertSlot`, `SendSlot` already in `node.rs`'s test imports |
| 5 | **import gap** — `test_track` lives in `types::testutil`, not in `mod tests`. Ruling below. |
| 6 | clean — `LoopSpec` verified `Copy` (`transport.rs:4`) |
| 7 | **borrow shape** — `node_playhead` returns `LoopSpec` by value; `frame_pos` takes `&LoopSpec`. Ruling below. Fixture names in its tests are placeholders (the plan says so). |
| 8 | clean |
| 9 | fixtures `render_one_block_for_tests` / `source_samples_for_tests` do not exist; the plan's self-review already says the implementer builds them. `Clip` field names verified (`types.rs:292`). |
| 10 | clean — `crate::midi::MidiClip` verified re-exported (`midi/mod.rs:69`) |
| 11 | see 9 ↔ 11 |
| 12 | **signature gap** — `migrate_clip_targets_to_players` needs the tracks to resolve `instrument_of_track`, but the signature takes none. Ruling below. |
| 13 | clean |
| 14 | clean |
| 15 | clean |

### Pre-flight rulings

- Ruling: Task 3's tests use `session_with_one_track("t1")` (the module's real fixture), adding players to `session.store.players` directly — not a new `test_session()`. Why: a second fixture for the same shape is duplication the review rubric flags. Cost if wrong: none, it is a test-local name.
- Ruling: Task 5's tests import `use super::testutil::test_track;`. Why: that is where the helper lives. Cost if wrong: a compile error the implementer fixes in one line.
- Ruling: Task 7's `node_playhead` returns `LoopSpec` by value and call sites pass `&track_lp` to `frame_pos`. Why: `LoopSpec` is `Copy` and 24 bytes; returning a borrow would tie the return to two different lifetimes (`lp` and a `'static` `OFF`). Cost if wrong: a borrow-checker fight the implementer resolves by reverting to the old `&'a LoopSpec` shape, which also compiles.
- Ruling: Task 9's player loop pushes the row at the tail of each match arm rather than at the loop tail, so Task 10's `MidiClip` arm slots in without restructuring what Task 9 wrote. Why: it keeps Task 10 additive. Cost if wrong: Task 10 restructures ten lines.
- Ruling: Task 11 is expected to be small — `player_fire` already carries `looping` from Task 9, so Task 11's deliverable is `set_trigger_mode` plus the four behavioural tests. It keeps its own dispatch because those tests are its gate. Why: the plan's task text says as much; this records that a thin diff there is correct, not a shortfall. Cost if wrong: nothing — the reviewer checks the tests, which are the substance.
- Ruling: `migrate_clip_targets_to_players` takes a fourth parameter, `tracks: &[TrackState]`, and `instrument_of_track` is a closure over it. Why: the function must resolve the clip's source track's `instrument_id` and must not reach for a global. Cost if wrong: the migrated player has no instrument and a migrated pad is silent — caught by Task 12's first test, which asserts the exact `instrument_id`.

Scan complete. No conflict blocks execution.

## Task log

Task 1: dispatched (implementer, sonnet) — BASE f2b9f74
Task 1: implementer DONE — commit 3c42e4e (4/4 tests, build clean). Review dispatched (sonnet), diff f2b9f74..3c42e4e
Task 1: review clean (spec ✅, quality Approved, 0 Critical/Important)
Task 1: minor (deferred): player.rs:44-48/104-118 — two manual `Default` impls are clippy `derivable_impls`; `#[derive(Default)]` + `#[default]` on `PlayerSource::None` would match `TriggerMode`'s own form in the same file
Task 1: minor (deferred): player.rs:7-9 — module doc states `From<&Player> for MixNode` in the present tense; Task 4 makes it true
Task 1: ⚠️ resolved by controller — the reviewer could not verify that a later task adds `Store::players` and the `MixNode` producer. Task 2 adds `Store::players`; Task 4 adds `From<&Player> for MixNode`. Not a gap.
Task 1: complete (commits f2b9f74..3c42e4e, review clean)
Task 2: dispatched (implementer, sonnet) — BASE 3c42e4e
Task 2: implementer DONE — commit a508902 (16/16 audio::project, v3_migration 4/4, control 106/106 single-threaded)
Task 2: implementer widened scope into control/mod.rs (open_project_epoch, create_project_at, project_changed_payload) — brief's line numbers were stale. Flagged to the reviewer as the thing to scrutinise, with the document-swap-completeness risk named.
Task 2: note — parallel `cargo test --lib` is flaky in this sandbox (no audio backend); single-threaded is the reliable run. Matches docs/backlog/ci-hardening.md item 5.
Task 2: review dispatched (sonnet), diff c111d98..a508902
Task 2: review clean (spec ✅, quality Approved, 0 Critical/Important). Reviewer verified the control/mod.rs widening was necessary, not creep: audio/project.rs::open_project is a dead stub, and create_project_at without players.clear() would leak players across a document swap.
Task 2: Ruling: SessionSnapshot (control/snapshot.rs) gains a `players` field, and both Session::from_snapshot and Session::restore_from_snapshot carry it — done in TASK 3, not deferred. Why: the reviewer found it latent-but-real, and it is worse than latent — engine::rebuild's phase 1 assembles from the PUBLISHED SNAPSHOT (`self.published.lock().clone()`), so Task 9 cannot build a player row at all if the snapshot has no players; and F-3's panic-rollback path would leave store.players un-rolled-back the moment Task 3's ops run through transact. Cost if wrong: Task 9 assembles nothing and the defect surfaces as silent pads.
Task 2: complete (commits c111d98..a508902, review clean)
Task 3: dispatched (implementer, sonnet) — BASE a508902
Task 3: implementer DONE — commit 680b719 (cargo test --lib --test-threads=1: 1418 passed / 0 failed; journal_replay gate passes)
Task 3: implementer self-flagged (a) shallow duplication vs the Track prop helpers, (b) wildcard `other =>` in read_player_prop/write_player_prop against the crate's anti-drift rule. Both handed to the reviewer to judge on merit.
Task 3: review dispatched (sonnet), diff 1aa6415..680b719 (44 KB)
Task 3: review — spec ✅, quality "Needs fixes": 2 Important, both plan-mandated (they came from my plan text verbatim).
Task 3: Ruling: finding 1 UPHELD — `effect.rebuild = !matches!(path, TriggerMode)` also rebuilds on Name. The Track arm treats Name as document-only and does not rebuild; a pad rename must not rebuild the audio graph. Narrow to `Raw | PlayerSource | Gain | Pan | Muted`. Note for Task 9: once players are in the slot map, Gain/Pan should become `effect.param_writes` (an atomic store) rather than a rebuild, exactly as the Track arm does — a fader drag that rebuilds the graph is the defect §10 exists to prevent. Cost if wrong: a rename silently stops reaching the graph — covered by the per-path rebuild tests added in this fix round.
Task 3: Ruling: finding 2 UPHELD — the wildcard `other =>` in read_player_prop/write_player_prop is replaced with explicit unions of the remaining PropPath variants. Why: op.rs's own doc states PropPath is closed so a variant change is a compile error at every use site (§4.6 anti-drift), and all six sibling helpers enumerate explicitly. My brief was wrong to write a wildcard. Cost if wrong: none — this is strictly stricter.
Task 3: fix round 1/5 dispatched (resume implementer)
Task 3: fix round 1/5 (3 addressed, 0 open — rebuild allow-list, explicit prop unions, rebuild-rule test; commits 680b719..a375f42)
Task 3: complete (commits 1aa6415..a375f42, review clean)
Task 4+5: BATCHED into one dispatch. Why: both are additive, single-file, complete-code-in-brief changes (node.rs, types.rs) whose review surface is one coherent unit — players enter the compiler's input and the slot map. They have no code dependency on each other, only the by-construction agreement the pre-flight scan rowed (raw player emits no sends AND consumes no send lane).
Task 4+5: dispatched (implementer, sonnet) — BASE a375f42
Task 4+5: implementer committed Task 4 (f0bfef2) then DEADLOCKED waiting on its own background Monitor task; it did not respond to a resume telling it to run the focused suite in the foreground instead, and reported nothing twice. Stopped it.
Task 4+5: Ruling: dispatch a FRESH implementer to finish Task 5 rather than resuming further. Why: the stall is a harness deadlock, not a reasoning failure — Task 5's 110 lines are already written and uncommitted in types.rs, so the fresh agent verifies, tests, commits and reports rather than re-implementing. Cost if wrong: the uncommitted work is wrong in some way the fresh agent must notice — which is exactly what it is being told to check first.
Task 4+5: fresh implementer dispatched (sonnet) — Task 4 at f0bfef2, Task 5 uncommitted
Task 4+5: fresh implementer DONE_WITH_CONCERNS — commits f0bfef2 (T4) + eb00b42 (T5); audio::types 10/10, audio::node 10/10, build clean. Task 5's inherited diff was a verbatim match to the brief.
Task 4+5: ⚠️ resolved by controller — implementer noted neither `_with_players` family has a caller outside its own module. Expected: Task 9 wires both into engine::rebuild's phase 1 (mix_nodes_with_players) and phase 2 (derive_slots_with_players / mixer_slot_count_with_players / derive_send_slots_with_players / send_slot_count_with_players). Not a gap; carried into Task 9's dispatch.
Task 4+5: review dispatched (sonnet), diff 78b2c12..eb00b42
Task 4+5: review clean (spec ✅ both, quality Approved, 0 Critical/Important). Reviewer verified the V-6 cross-task agreement directly, including the duplicate-send-id trap: send_slot_count_with_players sums rows and derive_send_slots_with_players counts rows, so neither reads the map's len() and they cannot drift.
Task 4+5: minor (deferred): node.rs:3 and :26 — two pre-existing doc lines still call the Player producer "a future cut's" / "a future `Player` producer (V2)". Now stale; folded into Task 9's dispatch as a one-line fix.
Task 4+5: minor (deferred): node.rs:218-227 — the prefix test proves id+kind equality only; MixNode has no PartialEq, so a future refactor that rebuilds rather than reuses mix_nodes() could regress a field silently.
Task 4+5: minor (deferred): node.rs:143 — test name says "source_kind_node" but asserts MixNodeKind::Player. My brief's wording; harmless.
Task 4+5: Ruling: Task 4's RED-before-GREEN is unconfirmed (the original implementer was stopped before reporting, and its commit combines tests + impl). Accepted as-is: the tests assert specific values against code that is verbatim from the brief, and re-manufacturing a RED would cost a task cycle to prove a process point about code the review has already read. Cost if wrong: a test that cannot fail — bounded, since the reviewer read the assertions.
Task 4+5: complete (commits 78b2c12..eb00b42, review clean)
Task 6: dispatched (implementer, sonnet) — BASE eb00b42
Task 6: implementer DONE — commit f4e171b (audio::clock 8/8, build clean)
Task 6: CARRY-FORWARD INTO TASK 7 (raised by the implementer, and it is real): `discont` is `swap`ped in `playhead()`, so the FIRST reader consumes it. Today's mixer calls `track_playhead` TWICE per track per block — once in the prologue (mixer.rs ~695, to decide whether the live node owes an all_notes_off) and again in the strip body (~719, to place the runs). That was safe with the overlay because `engine.rs` swapped `launch_discont` once and passed the same LaunchPlayhead value down to both calls. With the clock table it is NOT safe: the prologue would eat the flag and the strip body would see false, so a fired pad's live node would never get its all_notes_off. Task 7 must call `playhead()` ONCE per node per block and carry the result (pos, loop, on, disc) to the strip body — `RtTrack::win` already exists for exactly this kind of block-lifetime carry.
Task 6: review dispatched (sonnet), diff 264303b..f4e171b
Task 6: review — spec ✅ with 2 Important, both plan-mandated.
Task 6: Ruling: finding 2 UPHELD, AND IT INVALIDATES MY EARLIER CARRY-FORWARD. The reviewer proposed "slot->clock must stay 1:1" as the acceptance criterion. That is WRONG: a scene clock is deliberately bound to MANY slots (every track the Region names), so 1:1 cannot be the rule. The real defect is that `discont` is consumed per-READER at all. Today's engine calls `SharedRt::launch_overlay()` ONCE per block, swapping `launch_discont` a single time, and hands the same LaunchPlayhead value to every FLAG_LAUNCH track — so all of them see the discontinuity. My clock table broke that: with N tracks on one scene clock, the first reader eats the flag and the other N-1 live nodes never get their all_notes_off, which is a hanging note on every track of a fired scene but the first. Fix: `ClockTable::begin_block()` latches every clock's `discont` once per block; `playhead()` reads the latch non-destructively and is therefore idempotent for any number of readers. Cost if wrong: begin_block is forgotten at a call site and discontinuities never fire at all — louder and easier to catch than the silent per-reader race it replaces, and the new many-slot test pins it.
Task 6: Ruling: finding 1 UPHELD — drop `playhead()`'s dead `lp` parameter AND the unread `Playhead::looping` field, and fix the two doc comments to say the caller consults the arrangement's LoopSpec itself when `is_transport` is true. Why: nothing reads either; a clock's own loop is handled inside advance(). Task 7's node_playhead still takes `lp` at the mixer level, where it IS used to build the returned spec. Cost if wrong: a later cut needs `looping` on the Playhead and re-adds one field.
Task 6: fix round 1/5 dispatched (resume implementer)
Task 6: my fix message stated the wrong expected value for the multi-period overshoot test (pos == 90; I carried the reviewer's hand trace, which started from pos=50, but fire() resets pos to start=0, so 340 % 100 == 40). The implementer ran it, saw `left: 40, right: 90`, and used the verified 40. Correct call.
Task 6: fix round 1 re-review dispatched (haiku), diff f4e171b..9a75eb3
Task 6: fix round 1/5 (2 findings + 4 tests addressed, 0 open; commits f4e171b..9a75eb3). Re-reviewer traced both latch hazards: a fire landing after begin_block() is deferred one block, not lost (same one-block property the old launch_discont overlay had), and begin_block stores rather than ORs, so a discontinuity cannot repeat forever.
Task 6: complete (commits 264303b..9a75eb3, review clean)
Task 7: perf BASELINE being measured on origin/main in the main worktree FIRST, alone, before any Task 7 build competes for CPU. Same-sitting pair per CLAUDE.md.
PERF BASELINE (origin/main @ 6fe1112, main worktree, measured alone): 404.2 us — bare, 32 tracks, best of 3 runs, spread 4.2%. Branch budget = 404.2 x 1.3 = 525.
Task 7: dispatched (implementer, opus) — BASE 9a75eb3. Most capable model: five files, the RT contract, and a behaviour-neutrality requirement.
Task 7: implementer DONE_WITH_CONCERNS — commit d188210. --lib 1442 passed/0 failed/2 ignored, all 13 --tests targets green (--test-threads=1); clippy 130 warnings before AND after.
PERF BRANCH: PERF-VERDICT: OK 245.1 us (budget 525.0, bare at 32 tracks, best of 3, spread 4.7%). Baseline was 404.2 on origin/main. Under budget; the gap is within the documented 260-408 us same-code spread on this machine, so read it as "no regression", not "a speedup".
Task 7: implementer reported FOUR deviations from the brief's literal code, each because the literal version was NOT behaviour-neutral: (a) RtGraph::new needs a sized, playing table (default() is zero slots and would silence every non-engine graph incl. the bounce); (b) node_playhead needs a third case where a STOPPED own-clock rejoins the arrangement — that is LaunchPlayhead::ended, and without it a launch drops out at the press and hangs a note at the end; (c) stop_launch_overlay must NOT release the slots, or nothing reads the flush; (d) set_transport_playing mirrored in the output callback (the only reader) rather than at ~70 playing.store sites. Plus a new ClockTable::carry_over — the overlay lived on SharedRt and survived rebuilds; a per-graph table only does if copied.
Task 7: ONE pre-existing test edited, one line, no assertion changed: engine::tests::audio_track_insert_plugin_processes_the_clip_audio renders a rebuilt graph directly, bypassing the callback, and never set `playing`.
Task 7: residual delta reported by the implementer — a rebuild during a sounding scene rewinds it by ~one callback block (queue latency). The old overlay had no such rewind. Handed to the reviewer as the key behaviour-neutrality question.
Task 7: note for Task 9 from the implementer — "a stopped clock rejoins the arrangement" is right for a scene, probably wrong for a player.
Task 7: review dispatched (opus), diff 525f507..d188210
Task 7: review — spec ❌ (structure all correct; three behaviour deltas), quality "Needs fixes". 4 Important. Reviewer independently verified all four implementer deviations as true+necessary+correct, incl. deviation (d)'s "one reader" claim across 74 playing.store sites AND the adoption-ordering detail it depends on (graph adoption at engine.rs:516-527 happens BEFORE the mirror at :732, so a fresh graph never renders against a stale transport flag).
Task 7: Ruling: findings 1, 2, 3 UPHELD, fix now. All three are strict losses of behaviour the pre-swap engine had, and neutrality is this task's defining requirement.
  1. The `ended` flush frame is gone whenever the transport is stopped: engine.rs:737 gates on any_running(), so when a scene clock stops the next block renders nothing, begin_block never runs, and prime_live's all_notes_off never fires. The deleted code did the opposite on purpose (overlay_ended.then_some(LaunchPlayhead{ended:true}) forced one more render). Reachable on the mainline: FireOrigin::Preview fires without starting the transport. Worse, ClockTable::stop's own new doc claims the flush is preserved.
  2. ClockTable::stop fabricates a discontinuity for a clock that was never running — the exact claim of the deleted `ending_an_idle_overlay_is_a_no_op` test. Old end_launch used `if !swap(false) { return false }`. Reachable: stop-all during the ~8ms window between a scene ending and the drive thread releasing slots sends a spurious all_notes_off that cuts arrangement notes mid-song.
  3. node_playhead case 3 returns only ph.discontinuity, dropping the transport's. The old code returned the transport's for a flagged track with no overlay. The `discontinuity` parameter being unused on that branch is the smell.
Task 7: Ruling: finding 4 (one-block rewind on rebuild) DEFERRED to Task 8, which has to revisit clock reconciliation for per-binding clocks anyway — the reviewer explicitly offered this if I book it, and I am booking it. BUT the code comment's stated reason must be corrected now: it claims the retired-graph-advanced case "could not be distinguished" from a fire landing in the window, and the reviewer showed a monotonic fire counter distinguishes them with one relaxed load. A wrong reason in a comment is worse than no comment. Cost if wrong: a click plus a re-emitted note-on on rebuild-while-scene-sounds, bounded to one block, until Task 8.
Task 7: fix round 1/5 dispatched (resume implementer, opus)
Task 7: fix round 1 implementer DONE — commit 5c7ef0a. --lib 1448 passed/0 failed/2 ignored, 13 --tests targets green; clippy still 130 (unchanged from origin/main). PERF: OK 231.8 us (budget 525.0, spread 1.7%).
Task 7: implementer reverted each of the three production fixes individually to confirm its test goes RED without it. 7 new tests, incl. both deleted overlay tests re-homed for real (finding 1's at ENGINE level driving OutputCb::render, which is the integrated path the review said nothing covered) and the first coverage anywhere of prime_live all-notes-offing there. It also conceded its concern #3 was wrong and dropped the redundant clock_of load.
Task 7: fix round 1 re-review dispatched (sonnet), diff d188210..5c7ef0a
Task 7: fix round 1/5 (3 Important + 4 required comment/doc/test items addressed, 0 open; commits d188210..5c7ef0a). Re-reviewer traced the flush_pending render-gate cycle for non-termination (exactly one extra block, then the gate falls), confirmed advance()'s natural-end path still sets discont independently so finding 2's guard does not reintroduce finding 1, and confirmed case 3's OR does not over-fire on cases 1 or 2. It also verified the per-fix RED claim was real rather than an overstatement by matching cited panic lines to actual fn declarations.
Task 7: complete (commits 525f507..5c7ef0a, review clean)
Task 8: dispatched (implementer, opus) — BASE 5c7ef0a. Carries the finding-4 debt booked here: the monotonic fire counter that makes rebuild-time clock reconciliation exact.
Task 8: implementer DONE_WITH_CONCERNS — commit 2115f73. --lib 1465 passed/0 failed (+17 new), 13 --tests targets green, vitest 1383 passed, clippy 130 (unchanged). Every claim verified RED by reverting piece by piece.
PERF Task 8: OK 411.1 us (budget 525.0, spread 5.7%). PASSES, but see the next line.
Task 8: MAJOR MEASUREMENT FINDING — the implementer saw HEAD ~240 us vs branch ~390 us in a same-sitting A/B, did not accept it, and bisected: adding ONE UNUSED AtomicU64 field to ClockTable on a clean tree also produces 400.0 us. The harness is bimodal on codegen layout near audio::clock; the absolute number reflects layout, not work. This retro-explains Task 7's 231.8 vs the 404.2 baseline too — neither was a speedup or a regression. Consequence: a `git bisect run` driven by this gate WILL lie in this area. Needs a docs/backlog/ci-hardening.md item and a note in docs/TRAPS.md — booked into Task 15.
Task 8: NO test was deleted. The brief expected one asserting the hardware fire seeks the transport; the repo has none — that behaviour was never covered anywhere, which is part of why it survived. Covered now in the negative.
Task 8: scope beyond the brief's three files, each explained in the report: the drive thread's release edge; the runtime's singular overlay_id/audible_tracks; rebuild's slot rebinding (was ONE Vec that the last fire overwrote — wrong for N scenes); one frontend line clearing the launcher indicator for origin === "hardware".
Task 8: `stop` counts as a control-side write alongside `fire` in the reconciliation counter — a stop landing in the rebuild window would otherwise be undone by the reconciliation. Sound, and beyond what I specified.
Task 8: pre-existing and untouched — TransportAction::Stop still releases slots in the same breath as the stop, dropping the flush frame. Recorded for the final review.
Task 8: review dispatched (opus), diff c47e884..2115f73
Task 8: review — spec ✅ (brief + carried-forward reconciliation), quality Approved. 1 Important, 8 Minor. Reviewer verified the reconciliation's three hard parts (pair by binding id, the generation guard, bump-counter-before-state) against the [I1] retry path and the multi-adopt loop rather than on faith, and confirmed all four scope extensions were necessary — notably that base rebind read runtime().audible_tracks(), ONE Vec the last fire overwrote, so with two scenes sounding a rebuild handed the last fire's tracks to it and dropped the other's.
Task 8: Ruling on the perf claim — the null control (base + one unused AtomicU64 -> 400.0 us) DOES license "the branch's 411 us is not attributable to this change", and the reviewer independently confirmed the diff adds no per-block work (the harness project has no launch bindings, so n_clocks there is 1, DOWN from 2 — the branch does strictly less per-block work than base in the exact config the gate measures). But the report's stated MECHANISM ("bimodal on codegen layout") is one binary per variant with no replication and is stated more strongly than the evidence carries. Task 15's ci-hardening entry must say what was actually shown, not the hypothesis. Cost if wrong: we blame layout for a real regression later.
Task 8: Ruling — budget stays 525 for the remaining tasks. But if the mode is per-binary, tasks 9-14 will keep reporting ~400 and spend the headroom quietly. Standing instruction from here: a perf FAILURE in the 400-500 band must be re-measured with the null control (add an unused atomic to a hot struct on a clean tree) before it is believed as a regression.
Task 8: Ruling: Important 1 UPHELD, fix now — stop_scene's doc asserts releasing the slots there is safe because it is called ~8ms after the clock stopped. False for the Gate release path (launch.rs:581-589), which calls it while the clock is STILL RUNNING, so the discontinuity stop latches is never read and a live node keeps the note. Behaviour is pre-existing; the NEW thing is a comment asserting an invariant that does not hold, which is what lets the next reader delete the guard.
Task 8: Ruling: Minor 3 RAISED TO IMPORTANT, fix now — engine.rs:2035-2050 drops the `prev` guard and re-acquires at :2052. A fire_scene landing between the two acquisitions writes slot bindings into prev.clocks that the carry loop already read, so those bindings are LOST FOR THE LIFE OF THAT GRAPH and the track plays the arrangement instead of the scene. reconcile_adoption recovers the clock state but not the bindings. Base was accidentally immune (it rebound from audible_tracks, set before the fire). The reviewer called it Minor; I hold the plan context and a scene silently not claiming its track for a whole graph generation is not minor. Mechanical: hold ONE MutexGuard.
Task 8: Ruling: Minor 6 RAISED TO IMPORTANT, fix now — deleting a binding while its scene sounds leaves the track on TRANSPORT_CLOCK in the fresh table with no discontinuity anywhere, so a live node bound to it hangs a note. Same class as Task 7's three flush-frame findings, which were all Important. The new behaviour (the scene stops) is right; it just needs the flush every other ending gets.
Task 8: Minors 2, 4, 5 folded into the same fix round (all mechanical): the level-triggered release edge can fill an 8-slot channel shared with FireCmd::Start and silently drop a pad press; two stale doc references to deleted APIs, one of which (lib.rs:148) still describes the defect this task deleted; duplicate binding ids collapse silently in the scene_clocks HashMap while n_clocks counts them.
Task 8: Minor 7 BOOKED INTO TASK 13 — the one frontend change is uncovered, and launch.svelte.ts's overlay store is still singular, so with two scenes sounding the indicator shows the last one fired. Now expressible; Task 13 owns the renderer.
Task 8: Minor 8 BOOKED INTO TASK 15 — the six new launch.rs tests share the process-wide LaunchRuntime ledger and are correct only under --test-threads=1. Latent flake if the suite ever goes parallel; belongs with the ci-hardening item.
Task 8: fix round 1/5 dispatched (resume implementer, opus)
CONTROLLER RULING (owner asleep from Task 8 fix round onward, explicitly delegating): keep executing 9-15 autonomously, rule rather than stall, ledger every decision.
Ruling: the PR STAYS DRAFT. Task 15's step 4 says `gh pr ready 121`; I am not doing it. The owner ear-check (a raw WAV on a pad, bit-identical to browser audition, while the arrangement plays) is owed and only the owner can perform it. A PR marked ready while its own gate is unanswered asserts something untrue. Cost if wrong: the owner types one command in the morning.
Ruling: nothing gets merged. Not delegated, and outward-facing.
Task 8: fix round 1 implementer DONE — commit 2cf1597. --lib 1473 passed/0 failed (+9), 13 --tests targets green, vitest 1383, clippy 130 unchanged. PERF: OK 365.9 us (budget 525, spread 7.9%). Each of the 5 fixes reverted individually to confirm RED.
Task 8: NOTABLE — the implementer's own first draft of the cut/release split was WRONG and it caught it: gating the release on elapsed time is not a guarantee that a render block ran, because the drive poll is 8 ms and a block is 10.7 ms. Shipped version gates on a new ClockTable::flush_pending_for instead. That is the class of bug this whole cut exists to kill, found by the person who wrote it.
Task 8: fix round 1 re-review dispatched (opus), diff 529cc5f..2cf1597 (49 KB — unusually large for a fix round, and it restructures a release path, so new breakage is a live risk; told the reviewer to treat it as such and to trace every ending for leaked slots).
Task 8: fix round 1 re-review — all 5 findings ADDRESSED, but the finding-4 fix INTRODUCED a new Important defect. Reviewer confirmed the good parts by hand: cut/release split routes all four endings through cut_scene; flush_pending_for reads the SLOT'S OWN clock so one scene's flush cannot release another's; the source gate's slicing actually spans rebuild's whole body (checked fn boundaries at engine.rs:1759 and :2347); the orphan-clock condition is right in BOTH directions; release_finished_scenes is off every RT path and early-returns before allocating.
Task 8: Ruling: new Important UPHELD — launch.rs:472/480, `release_sent` is only forgotten when the id leaves the ledger, but the worker removes it from the ledger on its own thread. Re-fire inside the same 8 ms poll gap and `retain` KEEPS the id, so the second ending never enqueues a Release: stuck ledger, stuck launcher indicator, every later ending swallowed until a transport stop. Retriggering a pad as its clip ends is normal launcher use. Fix: clear on the ON edge instead of relying on the ledger transition.
Task 8: Ruling: Minor "the finding-4 test tests a copy of the loop" RAISED — it is not a coverage gap, it is WHY the bug above shipped: the test re-implements the drive loop's edge logic inline and the copy has the identical defect. A test that re-implements the code it covers is worthless, and here it demonstrably was. Factor into a helper both call.
Task 8: Ruling: Minor "engine.rs:2055 should also orphan a cut-but-unflushed clock" RAISED to fix-now — same hung-note class as finding 3, which I already raised to Important, reachable through a one-block door. One-token change: `is_on(prev) || flush_pending_for(prev)`.
Task 8: Ruling: Minor "orphan-bound slots bypass solo indefinitely" RAISED to fix-now — before this it was an 8 ms window; the orphan clock is never released, and solo/mute is a plain Set that schedules NO rebuild, so the window never closes on its own. Soloing a track would fail to silence a stranded one, indefinitely. Publish the orphan index and release it after its flush.
Task 8: Ruling: Minor "flush_pending_for proves a block BEGAN, not that the node READ" — DOC FIXED NOW, mechanism DEFERRED. The window needs a poll to land inside one callback between begin_block and one node's read; sub-millisecond. The airtight version is a blocks-rendered counter, which is a new RT-visible mechanism at the tail of an already-long task. But an overstated doc comment is the thing this plan has now been bitten by three times, so the doc must state what the code delivers. Counter booked as a ci/backlog follow-up in Task 15. Cost if wrong: one node misses one all_notes_off in a sub-ms race.
Task 8: Ruling: Minor "debug_assert_eq! panics dev builds on user data" UPHELD — I asked for "a debug_assert or a warn"; the reviewer is right that warn alone is the safer half, because launch_set_map accepts a whole LaunchMap from the frontend/MCP and duplicate ids can also arrive from a loaded project file. Warn only.
Task 8: fix round 2/5 dispatched (resume implementer, opus)
Task 8: fix round 2 implementer DONE — commit d62ee5f. --lib 1479 passed/0 failed (+7), 13 --tests targets, vitest 1383, clippy 130. PERF: OK 224.0 us (budget 525, spread 5.4%). Each of the 7 tests reverted individually to confirm RED.
Task 8: the implementer named its own two reasoning errors precisely: (1) it cleared the edge memory on the tidier lifecycle event rather than on the actual edge — "a level cannot clear an edge memory across an interleaving"; (2) its round-1 reasoning conflated position with audibility — a stopped clock returns its nodes to the arrangement for POSITION, but own_clock still overrides solo.
Task 8: closing the is_on||flush_pending_for guard surfaced the same hole one step out — an unpaid ORPHAN's debt was itself dropped by a further rebuild, since nothing pairs a clock with no binding. Found and fixed by the implementer, not by a review.
Task 8: PERF — a datum AGAINST the implementer's own hypothesis, volunteered: this round ADDS a GraphTables field and the gate returned 224.0 us, where the same branch read 366 and 378-411 before. So "bimodal on codegen layout" does not survive either; the numbers simply move. Only the null control's 160 us step stands as fact. Task 15's ci-hardening entry must say exactly that and no more.
Task 8: fix round 2 re-review dispatched (sonnet), diff 2cf1597..d62ee5f
Task 8: fix round 2/5 (all findings addressed, 0 open; commits 2cf1597..d62ee5f). Re-reviewer traced the finding-1 interleaving poll-by-poll and confirmed the ON-edge clear closes it WITHOUT re-opening level-triggering; verified the orphan chain TERMINATES (at most one orphan per rebuild, and flush_pending() forces the render arm even with the transport stopped, so the chain ends as soon as one block renders); confirmed one orphan clock for N stranded slots cannot split the flush, because begin_block latches once per clock and playhead only reads.
Task 8: minor (deferred, folded into TASK 9's dispatch): engine.rs:2082-2084's arm can deliver a SECOND all_notes_off one block after the retired graph delivered the first, cutting arrangement notes started in between. It is the accepted price of the mandated fix — a hung note is worse and unbounded — but neither the code comment nor the report names it, and an unnamed consequence in a comment is the thing this plan has been bitten by three times. One-line comment addition.
Task 8: minor (deferred, folded into TASK 12's dispatch): launch.rs:215-226 — a fire AND an ending entirely inside one poll gap still loses that ending's announcement. Strictly narrower than round 1's defect and self-healing (the next poll observing the clock on clears the memory). Doc line, not code.
Task 8: minor (deferred, for the final review): stop_drive_launch (launch.rs:892-902) lets a stale FireCmd::Release cut a scene that was re-fired after the release was queued. PRE-EXISTING, untouched by this diff, and the ON-edge clear neither creates nor worsens it.
Task 8: complete (commits c47e884..d62ee5f, review clean after 2 fix rounds)
Task 9: dispatched (implementer, opus) — BASE d62ee5f
Task 9: implementer KILLED mid-run by a session rate limit (resets 03:40 Europe/Oslo, 2026-08-29). Its last output was "Now the full suites:" — so it had finished implementing and was starting verification. 1295 lines were left uncommitted across ten files.
Task 9: Ruling: the controller committed that work as d1f8cdd, labelled UNVERIFIED in the commit message. Why: 1295 lines of uncommitted work is one `git clean` or one stray checkout from gone, and custody is not implementation. I ran `cargo build` (clean) and `cargo test --lib -- --test-threads=1` (1505 passed / 0 failed / 2 ignored, +26 tests over d62ee5f) purely as diagnostics, so the commit message could state what is and is not known. NOT verified: the --tests targets, vitest, clippy, perf-check. No report exists, so there is no TDD evidence, no self-review, and no account of edited tests.
Task 9: next step — a FRESH implementer verifies d1f8cdd against the task brief before trusting it, then finishes the run (integration targets, vitest, clippy, perf gate, report). It must be told explicitly that the commit is unreviewed and that its job starts with reading the diff critically, not with assuming it is right.
Task 9: fresh implementer DONE — commit a6403af on top of d1f8cdd (not amended). lib 1508 passed/0 failed/2 ignored, --tests green, npm 1383, clippy --all-targets 130 unmoved. PERF: OK 230.3 us (budget 525, spread 0.5%).
Task 9: verdict on the inherited draft — SOUND. Matches the brief clause by clause, did requirements A, B and C unprompted, and edited NO pre-existing test and removed NO assertion (only additive `player_clocks: Default::default()` fields in test modules). Two finds beyond the brief: player rows are appended AFTER song_end is computed (else a pad would declare the song's length), and a Playhead::exclusive notion without which every idle pad would sound at bar 1 of every playthrough.
Task 9: the finisher added three pins, each proved by mutating the code it pins — the scene poll must never release a player's slot; a soloed track must not cut a sounding pad; V-15's pin widened from "uses mix_nodes_with_players" to "the bounce must not read .players at all". Plus fixed player_stop reporting "unknown player" for a pad whose graph had not been rebuilt yet.
Task 9: FOUR concerns to carry — (a) no frontend wiring yet, so the owner's ear-check is not reachable from the UI until Task 13; (b) rebuild phase 1 is structurally untestable headless, so two assertions are source-text pins; (c) PDC can latency-shift a raw pad against a browser audition; (d) Stop leaves pads sounding, an undocumented decision.
Task 9: concern (c) is the one that matters and I have made it the review's headline question. compile_pdc pads every source path up to the slowest sibling's, so a raw player with NO inserts can still be delayed to match a track carrying a 2048-sample plugin. Samples identical, but late. V-16 says "a bit-exact copy of the source's samples at unity" — it does not speak to latency, and a pad that responds late is a performance defect even when the audio is bit-exact. The owner's ear-check depends on the answer.
Task 9: review dispatched (opus), diff 93a707b..a6403af (117 KB, 3 commits, one docs-only)
Task 9: review — spec ✅ (brief + requirements A/B/C), quality "Needs fixes". 3 Important. Reviewer independently verified the no-edited-test claim (`git diff d62ee5f..d1f8cdd | grep "^-.*assert"` returns nothing) and confirmed Playhead::exclusive and the song_end ordering as genuine finds.
Task 9: Ruling: Important 1 UPHELD, fix now — an idle player renders its clip EVERY BLOCK into its inserts and pre-fader sends. `on = false` is only consumed inside apply_fader_into, which zeroes AFTER the strip has run, and there is no early-out between mixer.rs:743 and :790. Worse, an off clock never advances, so a never-fired pad sits at pos=0 and walks 0..frames of its own clip forever. Three consequences: the owner's stated cost expectation (one atomic load per idle pad) is NOT met — it costs what a muted track costs, and the perf harness configures no players so nothing measured it; a pad with an insert has that insert continuously fed its own first ~10ms, so firing it starts from polluted state; a pad with a pre-fader send leaks that fragment into its bus continuously, which is the "every pad sounds at bar 1" defect `exclusive` was written to prevent, reached through the send path.
Task 9: Ruling: Important 2 — PLAYERS ARE EXCLUDED FROM PDC PADDING. compile_pdc fills declared/applied for every non-bus node that takes a slot, players included, so a raw player's empty chain gets track_pdc = max over ALL source tracks. Numbers the reviewer worked out: 0 samples plugin-free; 64-512 (1.3-11 ms) with one oversampled saturator; 2048-4096 (43-85 ms) with one linear-phase EQ — 2-4x past the ~20 ms where a triggered hit stops feeling attached to the press. The alignment argument does not transfer: PDC aligns material sharing a TIMELINE POSITION, and a player has none; its reference is the performer's hand and the audio already leaving the speakers. Delaying by max puts the pad AFTER the beat the performer aimed at. Send-edge compensation (RtSend::delay) is a separate mechanism and stays. Must also be recorded beside V-16 in the plan doc, because the owner hits it on the first ear-check against a real project. Cost if wrong: a raw pad is early relative to a heavily-compensated arrangement — which is what an instrument should be.
Task 9: Ruling: Important 3 UPHELD — nothing can stop a looping pad. clear_launch_audible and stop_launch_overlay both iterate scene_clocks only; a looping clock never ends itself; any_running() keeps the callback rendering with the transport stopped, indefinitely; and player_stop's own doc justifies its contract with "stop-all presses every pad unconditionally", describing a caller that does not exist. Escape must cut everything sounding, players included. Leaving pads sounding through transport Stop is right and stays (V-2) — but it must be recorded in docs/backlog/plan-v-players.md, not only in a task report.
Task 9: Ruling: the brief's third Step-1 test was implemented differently — proved through a real mixer::render at engine.rs:5470 instead of two production-shaped test hooks on ControlPlane. SIGNED OFF: it is the better test, and adding `render_one_block_for_tests`/`source_samples_for_tests` to ControlPlane would have put test scaffolding on the crate's front door.
Task 9: fix round 1/5 dispatched (resume implementer, opus)
Task 9: fix round 1 implementer DONE — commit 67ebe50. lib 1513/0/3 ignored, --tests green, npm 1383, clippy --all-targets 130 unmoved. PERF: OK 299.7 us (budget 525) but spread 24.6%, the noisiest run of the branch — noted, not chased, per the standing rule.
Task 9: THE IDLE-PLAYER NUMBER, measured at last — 32 idle pads, 512-frame block, release build: 134.64 us -> 1.98 us minimum (median 142.01 -> 2.03). So ~1.3% of the block budget was being spent producing silence. Ships as an #[ignore]d test `idle_players_block_cost` because the perf harness builds the OFFLINE graph and V-15 excludes players from it.
Task 9: my V-17 ruling was INCOMPLETE and the implementer's test caught it. Zeroing track_pdc alone is not enough — compile_pdc pads omitted slots to the MAXIMUM — and out_delay had to go too. Worse, with the row unpadded `ready = t_src` becomes a lie, so a player's send edges were under-compensated by exactly t_src: my ruling would have broken the one alignment it explicitly said it preserved. Edge delay now derives from the player's own applied chain latency. Cost if I had shipped it as written: a non-raw pad's reverb send arriving early against everything else in that bus.
Task 9: F1's early-out was placed IN the strip loop rather than above prime_live, against my instruction, and the reason is right: prime_live is what delivers a cut pad's all_notes_off, so hoisting past it would trade a wasted strip pass for a hung note in Task 10.
Task 9: fix round 1 re-review dispatched (opus), diff a6403af..67ebe50
Task 9: fix round 1 re-review — all 7 findings ADDRESSED (reviewer verified the V-17 send-edge arithmetic independently against bus.rs/pdc.rs and confirmed `applied` not `declared` is the right half, so G-5 still holds on the send path), but the fix INTRODUCED one new Important defect.
Task 9: Ruling: new Important UPHELD — the exclusive-idle `continue` at mixer.rs:797 skips tap_into_bus, which is the ONLY thing that advances a player's RtSend::delay line, and DelayLine has no reset. So the last `delay` samples of every press stay in the line and never reach the bus (the send copy of a one-shot is truncated at the tail), and the NEXT press replays them into the bus at its onset — the previous hit's tail on top of the new one. Exactly the compensated-send configuration V-17 was written to protect. The early-out's own safety comment reasons about pdc and out_pdc (both None under V-17) and misses the one line V-17 explicitly keeps alive. Fix: drain zeros through any snd.delay before continuing; the Some case is rare so idle pads with no compensated send keep the 1.98 us.
Task 9: Ruling: V-17 IS REFINED — a player routed (not sent) into a BUS is compensated to that bus's t_in; a player routed to MASTER is not. Why: the reviewer found out_delay zeroed unconditionally, including when node.output is a bus, which leaves the pad's dry arriving early into a group bus while its SEND into the same bus arrives on time — the pad smears against itself, and the two halves of V-17 contradict each other. Within a bus everything must arrive together or the mix smears; to master, "the pad answers the press" is what matters and that is the default and the ear-check path. Cost if wrong: a pad routed into a bus containing a linear-phase-EQ'd track inherits that latency — but the alternative is a smeared bus, and master stays at zero.
Task 9: fix round 2/5 dispatched (resume implementer, opus)
Task 9: Ruling (controller, fix round 2): the round-1 finding's fix is REFINED from "drain zeros through any snd.delay and discard" to "FLUSH the tail and emit it". Why: the finding names two defects — the send copy of a one-shot is TRUNCATED by `delay` samples, and the next press replays that tail. Draining-and-discarding fixes only the second. Zeros are exactly what the full strip produces for an idle row (`on` is false, so apply_fader_into zeroes `post`), so pushing one window of silence through the tail stages and emitting it is not new audio — it is the same tail delivered on time. Cost if wrong: a pad with a compensated send costs more than 1.98 us per idle block; rows with no delay line still take the bare `continue`.
Task 9: fix round 2 implementer DONE — commit 95a17ed. lib 1518/0/3 ignored (+5), clippy --all-targets 130 unmoved, perf OK 444.7 us (budget 525).
Task 9: Ruling: V-17 clause (b) as I wrote it in the brief was WRONG and the implementer was right to rewrite it. "A player row carries no delay line" was load-bearing for the early-out's safety argument, and item 1 of this very round gives a bus-routed player an out_pdc line. A ruling may not contradict the code the same round produces. Cost if wrong: the next reader deletes the flush on the strength of a stale safety comment.
Task 9: the perf gate FAILED TWICE before passing, at 625.3 and 640.6 — above the 400-500 band the standing rule covers, so the implementer did the full attribution in one sitting: clean branch HEAD 328.6/310.4/300.6, null control (unused AtomicU64 on ClockTable, clean tree) 389.8, own changes 625.3/640.6 hot then 495.5/491.7/444.7. Decisive argument beyond the null control: the harness builds the OFFLINE graph, V-15 keeps players out of it, so exclusive_idle is never true there and nothing added is reachable from what the gate measures. NEW FACT FOR TASK 15: the gate is also THERMALLY sensitive — do not measure right after the 110-second test suite.
Task 9: fix round 2 re-review dispatched (opus), diff ca312fd..95a17ed
Task 9: fix round 2 re-review — both targeted Importants CONFIRMED FIXED, and the reviewer reproduced the RED itself by stubbing the new guard to `if false` rather than trusting the report. flush_idle_tail checked line by line against the full strip: buffer lengths, window offsets, tap ordering, route_out indices, RT contract — no defect. bus.rs change confirmed bit-identical for non-players, and V-6 confirmed safe (node.rs:117 forces output: None for a raw player, so a raw pad can never acquire an out_pdc). But 2 new Important.
Task 9: Ruling: new Important 1 UPHELD, FIX NOW, and GENERALISED — the exclusive_idle early-out strands the INSERT CHAIN, and round 2's rewritten comment asserts it does not. A non-raw pad with a 2048-sample linear-phase EQ and an 800-sample one-shot: the clock goes off at the clip end, the row is skipped from block 2, and all 800 real samples are still inside the plugin. The WHOLE PRESS IS INAUDIBLE and surfaces at the next press's onset — the identical truncated-tail + contaminated-onset pair round 2 ruled on for DelayLine, one stage upstream. NOT pre-existing: the early-out landed in fix round 1 on this branch, and before it the strip ran every block so the inserts did flush. A regression of ours that swallows an entire press is not deferrable. Rejected the reviewer's "correct the comment and file it" minimum. Rejected also a third named-hazard case: enumerating hazards one at a time is what has now missed one twice. Ruling: a pad that has stopped being triggered IS STILL SOUNDING until its tail is out — per-row tail_frames (applied insert latency + out_delay + max send delay + tr.pdc) computed at graph build, a flush_left counter, the ORDINARY strip runs with the clip read skipped and `on` driven by flush_left, and flush_idle_tail is DELETED. Raw pads get tail_frames == 0 by construction (V-6: no inserts, no sends, node.rs:117 forces output None) so they keep the bare skip and the 1.98 us. Cost if wrong: a non-raw pad costs a strip pass for tail_frames after it stops; a reverb insert's unbounded tail is hard-cut at flush_left == 0, which is what a transport stop already does to a track's inserts and is documented, not fixed.
Task 9: Ruling: new Important 2 UPHELD — round 2 created the first player row that can carry an out_pdc AND the code that drains it, but proved them separately: compile_routing proves the number, mixer::render proves the SEND drain, nothing proves the DRY drain. On this branch inspection is the wrong standard for a newly enabled RT branch.
Task 9: Ruling: Minor 3 FOLDED INTO ITEM 1 — the guard names lines (out_pdc, send delays) and misses tr.pdc. Harmless today, but naming lines instead of asking "does this row still hold anything" IS the shape of Important 1. tail_frames subsumes it.
Task 9: Ruling: Minor 4 UPHELD — V-17 (b) says out_pdc exists "whenever the pad is routed into a bus"; both lines are built only when the delay is non-zero (bus.rs:94, engine.rs:2023).
Task 9: fix round 3/5 dispatched (fresh implementer, opus)
Task 9: fix round 3 implementer DONE — commit bf0cb58. lib 1524/0/3 ignored (+6), clippy byte-identical to 95a17ed, perf OK 377.1 us cold (spread 4.9%), idle_players_block_cost min 1.89 us (round 1 datum was 1.98, so the bare skip is unmoved).
Task 9: the implementer DEVIATED from my brief on where tail_frames is computed and it is RIGHT — RtTrack::recompute_tail_frames() called from RtGraph::with_buses, the single funnel every graph passes through, instead of engine.rs from the RoutingPlan. Two reasons, both mine to have seen: deriving from the plan is a SECOND source of truth for exactly the drift item 1 exists to kill, and it would have missed the player rows' sends, which are attached in phase 2 AFTER the loop my brief pointed at. Signed off.
Task 9: MY BRIEF SHIPPED A LIVE REGRESSION AND THE IMPLEMENTER CAUGHT IT. I specified the fader gate as `flush_left > 0 && audible_with_launch(...)` replacing `clock_on && ...`. Wrong in both directions: with flush_left held at tail_frames while the clock is ON, a plain track (tail_frames == 0) has its fader shut during normal playback; and the disjunctive repair `clock_on || flush_left > 0` is ALSO wrong, because an ordinary timeline track on a STOPPED transport has clock_on == false but is not exclusive_idle and nothing has stopped feeding it — its clip read still runs at a frozen base_pos, so that gate opened the fader for tail_frames after every transport stop, holding whatever sat under the playhead. Measured 0.0523259 where silence was required. Shipped gate: `clock_on || (flushing && flush_left > 0)` with `flushing = exclusive_idle`, pinned by a_stopped_transport_still_silences_a_track_that_owns_a_tail. THE WHOLE EXISTING SUITE PASSED UNDER THE BROKEN GATE — nothing else would have caught it. Lesson: a gate on the path EVERY row takes needs its own pin per row KIND, not per row state.
Task 9: item 2's tests had no possible RED against 95a17ed (round 2's guard did cover out_pdc), so they are coverage, not a fix. The implementer proved they discriminate by temporarily forcing the early-out back to a bare skip: both failed, 7.942 vs 8.556 energy and a contaminated first block. Reverted. That is the right way to earn a no-RED test on this branch.
Task 9: the "clippy 130" figure carried in PROGRESS has accounting the implementer could not reproduce; it measured lib 68 / lib-test 122 (66 dup) / 1 / 1 by stashing and re-running against 95a17ed in the same sitting. UNMOVED is established; the number 130 is not. Task 15 should quote the per-target split, not the total.
Task 9: fix round 3 re-review dispatched (opus), diff 95a17ed..bf0cb58. Headline question: an ordinary timeline track must be bit-identically unaffected in EVERY state, because this commit changed a gate on the path every row takes.
Task 9: MODEL POLICY set by the owner — do not use opus for everything. Implementers: 10 opus; 11, 12, 13, 14, 15 sonnet. Reviews: opus for anything touching src-tauri/src/audio/ or midi/ (9, 10, 11, 12), sonnet for 13 and 15, none for 14 (the controller interprets the number against a null control). Reason for the split: opus where a wrong answer is SILENT (RT path, migration -> a pad that just makes no sound), sonnet where failure is loud or bounded (chrome under ADR 0006, measurement, prose). Review stays opus on audio/midi regardless of who implemented, because the repeated failure here is a test that re-implements the logic it covers and passes while production is broken.
Task 9: fix round 3 re-review — NO CRITICAL. Headline question answered affirmatively and by construction, not by test-passing: every new expression collapses to the old one when exclusive_idle is false, and exclusive_idle is `ph.exclusive && !ph.on && !ph.is_transport` with `ph.exclusive = idx <= n_player_clocks` (clock.rs:551), a pure integer compare against a field fixed at construction. engine.rs:2268-2290 binds ONLY player slots into 1..=players.len(); scenes and the orphan clock are above the range; carry_over/reconcile_adoption copy per-clock state, never slot bindings. So no timeline track in ANY state can reach the flush path. The reviewer also mutation-tested the fader gate: unqualified `clock_on || flush_left > 0` fails exactly the new pin (1523/1), confirming my brief's gate was a live regression and the implementer's repair is the right one. 2 Important, 6 Minor.
Task 9: Ruling: Important 1 UPHELD — `!flushing` on the clip read (mixer.rs:909) is entirely untested; the reviewer removed it and 1524/0 still passed. The behaviour it protects is real and reachable: ClockTable::stop freezes the clock INSIDE the clip (Escape mid-press), not past its end, so without the suppression the frozen track_base re-reads one fragment every flush block through the chain and out the fader — fix round 1's defect, bounded to tail_frames, audible as a stutter over the tail. Both existing rigs miss it because they let the clip reach its natural end, where the frozen pos is already past length_samples and clip_sample returns zeros anyway.
Task 9: Ruling: Important 2 UPHELD, FIX NOW rather than leaving it for task 10 — render_live_into is not suppressed during the flush, and the round-3 comment claims the clip read is the only source difference. Frozen pos means an event in the window is re-queued once per flush block. Unreachable today (player rows carry no live node, engine.rs:2005 `_ => Vec::new()`), but task 10 is literally "MIDI players with an instrument no track owns" and is the next task: a MIDI pad stopped with a note-on at the frozen position would re-trigger it ceil(tail_frames/block) times. Fix while the reasoning is in view. The split matters: gate the EVENT QUEUEING on !flushing but KEEP calling node.process — the node's own tail is exactly what the flush exists to drain, so skipping render_live_into wholesale would be wrong.
Task 9: Ruling: Minor 3 RAISED to fix-now — tail_frames SUMS out_pdc and the send delays, but the post-fader taps are taken from `post` BEFORE out_pdc.process, so they are PARALLEL branches. True worst path is inserts + pdc + max(out_pdc, max_send). Never under-counts, so nothing truncates — but the comment justifies the over-count as free and it is not: V-17 makes a bus-routed pad's out_pdc and its send edge into the same bus EQUAL, so the window is up to 2x the tail, ~4096 extra frames (85 ms) of FULL-STRIP processing with one linear-phase EQ in that bus, after every pad release.
Task 9: Ruling: Minors 4, 5, 7 UPHELD (pin the untested line(&pdc) term; the funnel invariant rests on convention alone since mixer.rs:1594/1630 assign pdc and call insert_on AFTER RtGraph::new — debug_assert or soften the comment, implementer's call, and a debug_assert IS acceptable here because this is an internal invariant, not the frontend-supplied value the earlier warn-only ruling was about; V-17 (b)'s tail enumeration is garbled, out_pdc appears twice). Minor 6 DOCUMENT-ONLY in docs/TRAPS.md: flush_left does not survive a graph rebuild while insert NODES are reused by LiveNodeRegistry, so a rebuild inside a flush window strands the plugin pipeline — narrow, no worse than 95a17ed, not a regression.
Task 9: Minor 6 (decrement bookkeeping) NO ACTION — reviewer traced it and it is correct: flush_left -= w_len once per WINDOW, decrement outside the inner run loop, so a block larger than MAX_LIVE_BLOCK decrements once per window. Multi-window path unreachable in practice (MAX_LIVE_BLOCK 4096, cpal quanta 128-2048).
Task 9: fix round 4/5 dispatched (fresh implementer, opus). Kept at opus against the owner's cost-saving split because item 2 contains a genuine RT design decision (where to cut render_live_into), not a mechanical fix. Sonnet starts at task 11 as planned.
Task 9: fix round 4 implementer DONE — commit 2542640. lib 1527/0/3 ignored (+3). clippy per target lib 68 / lib-test 122 (66 dup) / 1 / 1, IDENTICAL to base measured same sitting. perf cold OK 434.0 us vs base 422.6 same sitting. idle_players_block_cost 2.27 us vs base 2.22 same sitting.
Task 9: BOTH CARRIED PERF FIGURES ARE STALE AND WERE NEARLY READ AS A REGRESSION. 377.1 us and 1.89 us are not reproducible on this machine today; unchanged base code reads 422.6 us and 2.22 us. Against the carried numbers round 4 would have looked like a 15% and a 20% regression. RULE, now absolute: only SAME-SITTING PAIRS mean anything, and a carried number from an earlier sitting is not a baseline. This is the third distinct way this gate has misled someone on this branch.
Task 9: MY BRIEF WAS WRONG THREE TIMES IN ROUND 4, all caught. (1) Item 1's test shape: I asked for a single-press energy assertion, which CANNOT BITE — tail_frames IS the row's whole path latency, so anything entering the strip at the flush start leaves exactly as the window closes; a re-read fragment is stranded for the rest of the press and only surfaces at the NEXT press's onset. A one-press version passes against the deleted gate. The test runs two presses. (2) I claimed a live node cannot be put on a player row before task 10. False: RtTrack.live is just a field and a test can set it the way out_pdc_pad sets out_pdc; only the COMPILER cannot emit one (engine.rs:2005). (3) "queued exactly once" is reachable only if the stop lands BETWEEN the render and the advance — advance skips an already-off clock, so the position freezes on the window just played, which is where a control-thread stopAllSound naturally lands. Stopping after advance freezes past the window and the event is never queued at all, so there is no bite.
Task 9: item 2 design decision SIGNED OFF — set_block_context gets the FROZEN pos during a flush. Reason (the implementer's, and it is right): it is the automation seam's "absolute base position of this run", the playhead genuinely is not advancing, and a fabricated advancing position would walk the node's ramp cursors over material nobody is playing.
Task 9: item 5's debug_assert BIT IMMEDIATELY on two pre-existing rigs (two_tracks_line_up_when_one_has_256_samples_of_latency, bypassed_insert_still_contributes_latency, both 0 vs 256). That firing is the proof it works. Fixed by making insert_on recompute and adding a recompute after the two post-construction `.pdc =` assignments. This is exactly the convention-only hole Minor 5 predicted, found the day it was pinned.
Task 9: the 8th parameter tripped clippy too_many_arguments (lib 68->69); folded pos/run/discontinuity/flushing into a LiveRun struct, render_live_into is 5 args, clippy back to baseline.
Task 9: ROUND 4 IS UNREVIEWED. Diff 3801c72..2542640. The controller stopped here because the owner's 5-hour window was at 83% used with a reset at 17:39; the review is a clean unit with committed input and no context dependency, so it is the right thing to hand over rather than rush.
Task 9: PROCESS — GATE RUNNING SPLITS OFF THE IMPLEMENTER. The opus implementer has run all four gates itself every round, sitting through a 110-second suite, a clippy run and a thermal cool-down, with all that output landing in opus context. From now: the implementer iterates to green and COMMITS; a SONNET gate-runner then runs the four gates against the committed tree and reports only the numbers, base and branch in the SAME SITTING. It may not conclude — a perf failure is reported and stopped on; the null control and the judgement stay with the controller.
Task 9: fix round 4 re-review dispatched (opus), diff 3801c72..2542640 — KILLED by session rate limit before producing a report; re-dispatched after the reset. No output was lost (the reviewer had produced nothing).
Task 9: fix round 4/5 re-review (opus, diff 3801c72..2542640) — ALL SEVEN ADDRESSED, no new Critical/Important breakage. The reviewer did not take "a test exists" for items 1 and 2: for item 1 it traced why the rig bites (stop() leaves pos alone and advance skips off clocks, so the playhead freezes at 64 INSIDE a 100-frame clip; the 64-frame control ends at the same frozen position where clip_sample returns zeros either way, so the two arms are identical WITH the gate and diverge by four re-read blocks without it, surfacing at the second press). For item 2 it checked both directions (remove !flushing -> queued 5 vs 1; skip render_live_into wholesale -> processed 1 vs 5) and confirmed both counters come from a real LiveInstrument through mixer::render, not a re-implementation. For item 5 it verified independently that the debug_assert cannot fire for an ordinary row (both production constructors finish mutating before with_buses; a bypass toggle arrives as a NEW graph via Op::InsertSetBypass -> rebuild = true) and audited every post-construction assignment site for recompute coverage — none uncovered.
Task 9: minor (deferred, for the final review): prime_live (mixer.rs:347, called at :778 before the row loop) still queues hardware MIDI-in events and can fire all_notes_off for a flushing row. CORRECT — live input is not derived from the frozen position so it cannot repeat — but it is a deliberate exception to the gate's "the row stops being FED" rule that no comment names. Worth a sentence when task 10 lands.
Task 9: complete (commits d62ee5f..2542640, review clean after 4 fix rounds)
Task 10: dispatched (implementer, opus per the owner's model policy) — BASE 3dabad0. Dispatch carried what the brief cannot know: row.live is an ordinary field, tail_frames funnels through RtGraph::with_buses and is now debug_asserted, the flush's fed-vs-processed split from round 4, prime_live as the named exception, V-15 (no players in the offline graph) and V-6. Gate running is split off per the new process rule: the implementer runs only focused tests and commits; a sonnet gate-runner takes the four gates against the committed tree.
Task 10: implementer DONE_WITH_CONCERNS — commit f7ab47e. 3 RED rounds + 2 mutation rounds; focused suites midi:: 171, audio::engine 146, audio::mixer+offline 74, control:: 424, 0 failed. Deviations it reported: &SessionSnapshot not &Session (rebuild phase 1 holds no lock); bank+nodes registry so the pad's node survives a rebuild with its state_rev; node_for_instrument EXTRACTED rather than node_for_track copied; no `continue` in the engine arm (it would have skipped inserts/PDC/output); `.sample` not `.at`; rate == 0 -> None rather than .max(1); plus retain_players and a player-namespace exemption in retain_tracks, because append_from's prune runs FIRST and would retire every pad's instrument on every rebuild.
Task 10: review dispatched (opus, diff 3dabad0..f7ab47e). Six headline questions, the first being the registry additions (does an ordinary track's retention stay bit-identical in every state) and the second a FACT I need in order to rule: is a bare MIDI pad's hard-cut release the same thing an ordinary MIDI TRACK already gets at transport stop, or is it new to players?
Task 10: gate-runner dispatched (sonnet) — the first use of the new split. It runs the four gates plus idle_players_block_cost against f7ab47e, measures the base at 3dabad0 in a THROWAWAY WORKTREE in the same sitting, and may not conclude.
Task 10: CAUTION on this round's perf pair — the reviewer and the gate-runner were dispatched in parallel, so a focused cargo test from the reviewer could land inside the gate-runner's measurement window. If the pair looks anomalous, re-measure with nothing else running before believing it.
Task 10: review (opus, diff 3dabad0..f7ab47e) — spec ✅, task quality APPROVED. All six brief deviations judged CORRECT on the merits, and two of them would have been DEFECTS had the brief been followed literally: the brief's `continue` in the engine arm would have skipped the inserts/track_pdc/out_pdc/output attachment that follows in the same loop body (stripping a non-raw MIDI pad of its chain and routing, and leaving tail_frames inconsistent with the row's lines — the round-4 debug_assert would have fired); and `rate.max(1)` would have converted every tick at 1 Hz and cached a node under `…@0`. The registry additions are required, not gold-plating: without `nodes` threaded through, the brief's own "state_rev must stay in the key" instruction is meaningless, because a fresh node per rebuild makes the key moot. node_for_instrument as an EXTRACTION rather than a copy is better than the brief: node_for_track is now a pure forwarding wrapper, so the two existing state_rev/activation tests now cover the player path's key too. Reviewer confirmed the per-track path bit-identical down to the log-line text. 2 Important, 5 Minor.
Task 10: HEADLINE QUESTION 2 ANSWERED — a bare MIDI pad's hard-cut release is AUDIBLY what an ordinary MIDI track already gets, but by a different mechanism, and the pad's is stricter. A track on a stopped transport is NOT skipped (clip read, render_live_into and node.process all keep running at the frozen base_pos); it is silenced by the fader gate, `clock_on || (flushing && flush_left > 0)` with flushing false for a track (mixer.rs:920). Its synth's release therefore decays into a muted buffer. A bare pad has exclusive_idle true and tail_frames 0, so it takes the BARE SKIP at mixer.rs:900 and the node is not processed at all — its voices FREEZE rather than decay. Also established: no row kind compensates a live instrument's own latency; LiveInstrument (dsp.rs:69-73) exposes no latency accessor and computed_tail_frames counts only insert latency, pdc and send delays.
Task 10: Ruling: the hard cut itself is NOT task 10's to fix — it is the documented V-17(b) consequence ("what a live node holds BEYOND its declared latency — a synth's release — is not in the number") and a track already sounds the same. Records as a V3 item (per-instrument release allowance or a minimum pad tail), NOT a task-10 defect. Cost if wrong: every MIDI pad clicks on release until V3, which is what a MIDI track already does today.
Task 10: BUT THE FROZEN-VOICE HALF IS MINE TO CHASE AND THE REVIEW DID NOT ASK IT. A track's voices decay into a muted buffer, so they are SPENT by the next press. A pad's are frozen mid-release. If prime_live's all_notes_off starts a release rather than killing the voice, that release is still queued in the node when the pad is next fired — and it resumes on top of the new onset. That is EXACTLY the truncated-tail + contaminated-onset pair this branch has now ruled on twice (round 1's DelayLine, round 2's insert chain), reached a third way through the plugin's own voice state. Folding it into fix round 1 as an INVESTIGATION with a two-press test if it bites — one press cannot see it, which is the lesson round 4 paid for.
Task 10: Ruling: Important 2 (two pads on one plugin instance instantiate two plugin instances) PARKED as plan-mandated and correct as built. Not merely because the brief says to key by player id: the mixer processes one live node per ROW, once per block, so two rows CANNOT share a node without double-advancing it. Sharing would mean the two pads become one row — a deck concept, a V3+ design change, not a key change. It is also not a regression: two TRACKS sharing an instrument_id already instantiate twice. Cost if wrong: a drum deck of eight pads on one Zyn loads Zyn eight times and splits its state, so a param edit lands on one copy. Goes to the design doc as a known limit with this reasoning, and to the final review.
Task 10: Ruling: Important 1 (the registry ordering is unpinned) ENTERS THE FIX LOOP. It is one assertion, but the defect it guards is an LV2/CLAP instantiate-and-destroy per rebuild on the control thread under the plugin main thread's run — and every failure return in live_source_for_player must precede node_for_instrument for it to stay correct. An invariant a later edit can break silently, with no test noticing, is exactly what a pin is for.
Task 10: THE GATE-RUNNER HIT THE DOCUMENTED BACKGROUND/MONITOR DEADLOCK — first run of the new split, and it ended its turn "waiting silently for the background 90-second idle sleep and the Monitor task", reporting nothing, having already spent 457 s. The process lesson is in this ledger and in the PROGRESS file and the brief said FOREGROUND ONLY, and it still happened. NEW RULE FOR EVERY GATE-RUNNER BRIEF: forbid the Monitor/wait task by name, and do NOT ask for a shell cool-down sleep at all — ask for THREE runs and the spread instead. A spread across three runs is better evidence than one number behind a guessed idle interval, and it removes the only thing the agent was tempted to background.
Task 10: gates GREEN at f7ab47e. lib 1535 passed / 0 failed / 3 ignored (+8 over 2542640's 1527). cargo test --tests: every target 0 failed (channel_properties 3, figma_invariant 2, identity_properties 2, journal_and_history 21, journal_replay 2, pitch_sidecar_parity 2, plugin_load_profile 7, pure_readers 3, real_models 2, snapshot_store 2, v3_migration 4). npm 1383 in 126 files, 0 failed. Clippy per target IDENTICAL branch vs base measured same sitting: lib 68 / lib test 122 (66 dup) / channel_properties 1 / journal_replay 1.
Task 10: THE PERF PAIR IS CONTAMINATED ON THE BASE SIDE, and the gate-runner did the right thing by reporting the fact without judging it: `ps aux` during base run 1 showed THREE unrelated `scripts/perf-check.sh --measure` processes it had not started (pids 1011403, 1021760, 1037406) — almost certainly orphans of the deadlocked first gate run. So base 474.4 / 442.4 / 431.2 us was measured under contention and is INFLATED; branch 265.3 / 263.7 / 261.8 us (all OK against budget 525, spreads 11.1 / 2.2 / 3.9%) was measured clean. Ruling: NO RE-MEASURE. The pair cannot show a regression that is not there — the contaminated side is the SLOW side, and a branch reading 262 us against a budget of 525 passes on its own terms whatever the base did. Re-measuring would cost 45 minutes to confirm a direction the contamination cannot have faked. Cost if wrong: none available — a regression hiding under a base that measured too SLOW is arithmetically impossible. The stray processes are gone now (`ps` clean), so task 11's pair starts uncontaminated.
Task 10: idle_players_block_cost branch min 3.26 us vs base min 3.17 us, same sitting — unmoved (3%, inside this harness's spread). Note both sides read HIGHER than round 4's sitting (2.27 / 2.22), which is the third independent confirmation that absolute figures do not travel between sittings on this machine. Only the pair means anything.
Task 10: fix round 1 implementer DONE_WITH_CONCERNS — commit 3da1437. Items 2 and 3 done (the registry-ordering pin earned by mutation, RED and revert both in the report; both stale doc comments fixed). Item 1, the investigation I added, BIT.
Task 10: THE FROZEN-VOICE CONTAMINATION IS REAL AND MEASURED. all_notes_off is a RELEASE in all four impls and in every one the release only HAPPENS inside process: PolySynth flips Held->Released as a flag (synth.rs:284-291), SamplerNode calls pool.release_all() (sampler_engine.rs:328), LV2 QUEUES CC 123 for the next process block (lv2_host.rs:1048), CLAP sets pending_all_off consumed at the next process (clap_host.rs:1126). The trait doc says it outright — "Release, not hard kill" (dsp.rs:61). A bare pad's node is never processed again, so the ramp never runs. Two presses through a real mixer::render: press 1 starts at 0.0 and attacks from silence; press 2 starts at 0.1096 — a step discontinuity at sample 0, i.e. a CLICK — and peaks 0.208 against 0.106. The stranded voice was at FULL SUSTAIN, not part-decayed. Every post-cut block is exactly 0, so the tail is truncated as well. Third instance of the same truncated-tail/contaminated-onset pair on this branch, after round 1's DelayLine and round 2's insert chain, reached this time through the instrument's own voice state. PolySynth::note_on prefers an Idle voice, so press 2 takes a DIFFERENT voice and the stale one survives alongside it.
Task 10: THE PROBE THAT SHAPES THE RULING — with a pass-through insert claiming 4096 samples of latency (longer than the 3840-frame release), the release decays audibly to exactly zero and press 2 is BIT-IDENTICAL to press 1. The existing flush machinery is sufficient. The row is simply never given a window that covers the instrument's own release.
Task 10: Ruling: FIX IT, AS A FIXED MINIMUM TAIL FOR ANY ROW CARRYING A LIVE NODE — not a per-instrument LiveInstrument::tail_samples(), and not a hard kill. Three reasons. (1) A per-instrument accessor cannot be honest for the nodes that matter most: LV2 has no tail concept at all and CLAP's is advisory, so the accessor would return a confident number for PolySynth and a guess for a plugin — a second source of truth that lies exactly where the cost is highest. (2) A hard kill at the freeze point is not implementable generically for the same reason: PolySynth and SamplerNode could reset their voices, LV2 and CLAP have no generic reset short of deactivate/reactivate, which is not an RT-path operation. (3) V-17(b) ALREADY documents the accepted consequence — an unbounded tail is hard-cut at the end of the window, exactly as a transport stop cuts a track's inserts — so a bounded allowance is the shape this design already committed to; it is what a non-raw pad's insert chain gets today. Cost if wrong: a MIDI pad runs a full strip pass for the allowance after each press instead of taking the bare skip, and an instrument whose release outlasts the allowance is still cut (but cleanly, mid-decay, not at full sustain).
Task 10: Ruling: THE ALLOWANCE APPLIES TO ANY ROW WITH live.is_some(), NOT ONLY PLAYER ROWS. It is inert for a track — tail_frames is only read under `flushing`, and flushing = exclusive_idle is false for every track row — so scoping it to players would buy nothing and add a second condition to a funnel whose whole value is that it has one rule. RAW PADS ARE UNAFFECTED: V-6 makes a raw pad an audio clip with no live node, so it keeps tail_frames 0, the bare skip and the measured idle cost, which is the owner's ear-check path.
Task 10: Ruling: the LV2/CLAP second symptom (the previous press's all-notes-off delivered at frame 0 of the NEXT press) needs no separate fix — the same window renders the queued CC 123 during the flush, which is where it was always meant to be consumed.
Task 10: fix round 2/5 dispatched (resume implementer, opus). Kept at opus against the owner's cost split because this is an RT-path change with a value judgement in it, not a mechanical fix; sonnet still starts at task 11 as planned.
Task 10: fix round 2 implementer DONE — commit 9c1c9cc. LIVE_TAIL_FRAMES = 4096 in rt.rs:580 with the argument at the definition; computed_tail_frames does strip.max(live), a floor never a sum, one rule for any row with a live node. Press 2 now starts at exactly 0.0 and is bit-identical to press 1. Item 5 confirmed by reading both hosts: the defect was never the QUEUEING (CLAP consumes pending_all_off at the top of the next process, LV2's CC 123 likewise) but that a frozen node had no next process; the window now runs it on the block right after the stop.
Task 10: THE IMPLEMENTER RETARGETED AN EXISTING TEST AND ARGUED IT WELL — a_flushing_row_does_not_re_queue_the_events_under_its_frozen_playhead failed on its own two literals (tail_frames == 256, processed == 5), both derived from the premise this ruling changes. It did NOT bump 5 to 6: with 6 blocks rendered, `processed == 6` asserts only that the node never STOPPED, silently deleting the "and stops at the end of the window" half. It now derives both from the window, renders past its end (69 blocks) and asserts processed == 1 + flush_blocks (65), with queued == 1 checked across 64 flush blocks instead of 4. Verified not hollowed by mutation (1 vs 65). I have made this the re-review's hardest question anyway — a test that fails because the behaviour legitimately changed is the most dangerous moment in a fix round.
Task 10: 96 kHz RESIDUE, carried and not fixed — 4096 frames covers PolySynth's ~80 ms release with headroom at 48 kHz but only about half at 96 kHz, so a stranded voice drops to roughly half amplitude instead of to nothing. Documented at the constant with the fix named. Ruling: DO NOT PLUMB THE RATE NOW. with_buses has 8 call sites and is the funnel a debug_assert already guards; its arity tripped clippy too_many_arguments once in round 4 at 8 parameters. Changing the signature of the one funnel every graph passes through, to fix a half-amplitude residue at a non-default rate, is disproportionate in a fix round. Goes to task 15 and the backlog with the exact fix named (the rate must reach with_buses; the constant becomes a duration). Cost if wrong: a 96 kHz user hears a half-amplitude click at the next press instead of none. I have asked the re-reviewer to verify the magnitude claim and price the rate-true version before I close this.
Task 10: fix round 2 re-review dispatched (opus), diff f7ab47e..9c1c9cc, covering both fix rounds. Five specific verifications, the hardest being (3) that a MIDI TRACK now carrying tail_frames 4096 instead of 0 is genuinely inert — no read of tail_frames outside the flushing path may exist — and (4) the retargeted test.
Task 10: fix round 2 re-review (opus, diff f7ab47e..9c1c9cc) — all findings ADDRESSED, no new Critical/Important. Verification 3 traced exhaustively: tail_frames has exactly TWO production reads (the debug_assert at mixer.rs:829 and the flush_left store at mixer.rs:898), and for a track row the store is a DEAD STORE because all three reads of flush_left are gated on flushing = exclusive_idle, which needs ph.exclusive, which is idx <= n_player_clocks — a clock property fixed at construction, and rebuild binds only player slots into 1..=players.len(). So a MIDI track's tail_frames becoming 4096 is genuinely inert. The reviewer also swept every rig that assigns .live post-construction (one, mixer.rs:2136, and its next statement is insert_on which recomputes) and confirmed the retargeted test keeps BOTH halves, with a four-block stop margin against the original's one.
Task 10: MY ROUND-2 RULING WAS WRONG ON THE MERITS AND THE RE-REVIEWER UNDER-GRADED IT. I ordered `max` with the reason "the allowance covers the instrument's release, which runs BEFORE the inserts and the delay lines the other terms already account for". Running before, in series, is exactly why they ADD. The reviewer flagged it Minor as a comment defect ("they do not share a window; the true drain for a live row with a 2048-frame chain is 3840 + 2048 = 5888") because I had ORDERED max and V-17(b) accepts a bounded cut. That grading is wrong and the fault is mine for pre-committing to the expression: the consequence is not a comment, it is release material STRANDED IN THE INSERT CHAIN when the window closes — truncated tail plus contaminated next onset, the FOURTH instance of that exact pair on this branch, and the one this very round was opened to fix. Task 9 round 4 already established the reasoning I contradicted: tail_frames is the row's whole path latency, so a fragment ENTERING the strip at flush start leaves exactly as the window closes — but release material keeps entering for the whole release, so the last of it enters at 3840 and needs `strip` more frames to get out.
Task 10: Ruling: THE EXPRESSION IS `strip + live`, NOT `strip.max(live)`. Series, because the release happens inside the node and its output then traverses the chain and the delay lines. Reverses my round-2 ruling and the test I ordered with it (the_live_node_allowance_is_a_floor_not_an_addition must become a SUM test). Cost if wrong: a live-node row runs `strip` more frames of full-strip processing after each press than it strictly needs — the same over-count I raised as Minor 3 in task 9 round 3, which is the cheap direction; the direction I ordered in round 2 is the one that swallows audio.
Task 10: Ruling: RATE-TRUE NOW TOO, reversing my deferral, because the proportionality changed. I deferred it when it meant opening the funnel for a half-amplitude 96 kHz residue; round 3 is opening that same expression anyway for a correctness fix, and the reviewer has since priced it: with_buses has SIX call sites (two production, four test), RtGraph::new delegates and its 51 call sites are ALL in mod tests so a 48 kHz default costs zero mechanical edits, and NEITHER production site lacks a rate in scope (offline::build_graph takes rate: u32; Control::rebuild has self.cache_rate, already used two statements earlier for the same graph). The one trap the reviewer found is the one that matters: computed_tail_frames is called from the build AND from render_impl's debug_assert, and render_impl has its OWN sample_rate parameter — using both would be two sources that disagree across a device rate change and fire the assert on the RT thread. Single-source shape: store rate on RtGraph and have the debug_assert read graph.rate. Cost if wrong: one more field on the graph and a wider round-3 diff on the funnel, guarded by the debug_assert and 461 audio tests.
Task 10: the 96 kHz magnitude claim CHECKED and slightly better than reported — PolySynth's release is LINEAR (synth.rs:18, :120, :240), so 4096 of 7680 frames completes 53.3% and the stranded voice sits at 0.467, not worse. No exponential tail making it uglier. Rate-true removes it entirely.
Task 10: fix round 3/5 dispatched (resume implementer, opus).
Task 10: SESSION PAUSED 18:12 CEST 2026-08-29, usage limit, resumes ~21:20. Fix round 3's implementer was still writing. Controller took custody as 4c15651, labelled UNVERIFIED, following the d1f8cdd precedent: cargo check --lib clean is the ONLY thing known about it; no report, no self-review, no test evidence, and it was still editing midi/playback.rs as the snapshot was taken. Round 3's brief was committed to BRIEFS/task-10-fix-3-brief.md at the same time — it had existed only inside a SendMessage, which dies with the session, and losing it would have cost the next agent the whole reasoning behind reversing the max ruling.
Task 10: fix round 3 CLOSED — the fresh implementer read both UNVERIFIED custody commits critically and reported that all three brief items were in fact present and finished in the ghost's tree, including the two-press-with-chain RED and the rename to the_live_node_allowance_adds_to_the_strip. Nothing deleted as wrong; four defects fixed, all in the doc/comment layer (a doc claiming "THE one clamp" while live_tail_frames clamped too, a duplicated RtGraph::new doc line, a mangled assertion string, two round-2 vocabulary leftovers). Commit 2439ff5. It also committed 19d8c44 first — 37 ukommitterte lines of round-3 test still in the worktree on top of 4c15651, one `git clean` from gone. Three REDs observed by the finisher itself, each mutation reverted.
Task 10: fix round 3 re-review (opus, diff 9c1c9cc..2439ff5) — APPROVE, all three items ADDRESSED, ONE Important. Every mutation claim in the finisher's report reproduced BYTE-FOR-BYTE (0.048259135, left: 8192 right: 4096, the third bail failing at its own new line). The reviewer also ran a mutation the finisher did not: LIVE_TAIL_FRAMES_AT_48K 4096 -> 2048 fails TWO mixer tests, so the allowance's MAGNITUDE is pinned behaviourally and not only by arithmetic. That is the strongest evidence in the round.
Task 10: Ruling: Important 1 ENTERS FIX ROUND 4. The reviewer replaced the production call site engine.rs:2531's self.cache_rate with a literal 48_000 and 1058 audio/control/midi tests stayed GREEN. Round 3 made the allowance rate-true and NOTHING PINS THE ONE ARGUMENT THAT CARRIES THE RATE. It fails silently by construction: render_impl's debug_assert reads graph.rate, so a wrong rate stored on the graph makes the invariant check AGREE with the bug — that consistency is exactly what makes the device-rate-change window safe, and it is also what removes the only alarm. Cost if wrong: a 96 kHz user gets a 4096-frame window over a 7680-frame release and the half-amplitude stranded voice that item 2 was opened to remove comes straight back, with no test and no assert to say so. Cheap: Control already lets tests set cache_rate directly (engine.rs:8011, 8307, 8336).
Task 10: Ruling: Minor 2 ALSO ENTERS ROUND 4. Nothing pins per row KIND that the raised tail is inert for a live MIDI TRACK; engine.rs:6358-6365 pins the raw-pad case only. The reviewer VERIFIED the inertness claim by reading (flush_left's three reads at mixer.rs:906/909/926 are all gated on exclusive_idle, which node_playhead sets only on the ph.exclusive branch), so this is a pin for a property that holds today, not a suspected defect. It is in because this branch has ALREADY shipped a gate that opened every track's fader for tail_frames after each transport stop and the whole 1518-test suite passed under it — the standing lesson is that a gate on the path every row takes needs a pin per row KIND, not per row state.
Task 10: Minor 1 NO ACTION — the_live_node_allowance_adds_to_the_strip derives its expectations from live_tail_frames, the helper it covers, which is the shape the brief warns about; but the reviewer's own 4096->2048 mutation showed the magnitude IS pinned behaviourally by the two mixer.rs tests. The arithmetic test is the weaker half of a pair and that is fine as long as the pair exists. Minor 3 (live_tail_frames(0) is a debug-only guard; recompute_tail_frames is pub and release-mode returns a 0 window) NOTE ONLY, to task 15 — reachable from test code alone today, and keeping ONE clamp so the docs stay true was the right call.
Task 10: ROUND-3 REPORT CONCERN 3 WITHDRAWN by the reviewer, and it is worth recording because I would have carried it. The finisher worried that Control::rebuild before a device opens builds at the clamped 48 kHz and is silent about a later rate change. But open_output calls rebuild() unconditionally on success, and rebuild -> ensure_loaded sets self.cache_rate = self.engine_rate() BEFORE the with_buses at 2531, so a device opening at 96 kHz does force a rebuild at the real rate. Residual exposure is a rate change that never routes through open_output, and the reviewer found none.
Task 10: fix round 4/5 dispatched (resume round 3's finisher, opus). TEST-ONLY round — no production behaviour changes; the brief tells the implementer that editing production to make either test pass means it has found a real defect and must stop and report instead. Gate-runner deliberately NOT dispatched in parallel this time: the task-10 pair was contaminated when reviewer and gate-runner overlapped, and the ledger's own caution says so.
Task 10: fix round 4 implementer DONE — commit 5963e47. TEST-ONLY as ordered: 153 insertions, ZERO deletions, no production line touched. audio:: 466, midi:: 171, control:: 424 = 1061 passed / 0 failed / 1 ignored.
Task 10: I VERIFIED ITEM 2's MUTATION MYSELF rather than taking the report, because the implementer volunteered that it took three attempts to find a mutation that is evidence (flushing = true fails on the wrong half — it also gates event feeding at mixer.rs:424; dropping the qualifier entirely is caught by the pre-existing clips-only stop test too, so it proves nothing about needing THIS test). Applied the live-row-specific refactor `flushing || tr.live.is_some()` to the fader gate at mixer.rs:926 and ran audio::: 465 passed, EXACTLY ONE failed — a_live_midi_track_row_never_flushes_after_a_stop_however_long_its_tail, block 0 after the stop, left 0.18921247 right 0.0. Reverted; tree clean. The pin bites and it is the only thing that does.
Task 10: Ruling: loopjam.rs:913 STAYS UNPINNED, and the implementer was right to report rather than contort. Its row is RtTrack::clips(...) with no live node, so the rate reaches only graph.rate and nothing on that path consumes it; a test there could assert code SHAPE and nothing else, which is the kind of test this branch keeps being burned by. Ledgered as the implementer asked: CORRECT FOR A REASON THAT COULD STOP BEING TRUE — if that row ever gains a live node the site goes silently wrong with no test to say so. To task 15 and the final review.
Task 10: controller fixed two text defects in 5963e47 (commit 63ea3a0): the premise assertion string was mangled with a run of spaces mid-sentence — the SAME defect class round 3's finisher fixed in the ghost's tree, so it is now twice in this task — and a comment headed "Well past the window" contradicted the two lines under it, which correctly said every block is INSIDE it. Inside is the point: that is where a wrongly opened fader would sound.
Task 10: NO RE-REVIEW OF ROUND 4. Test-only, zero production deltas, following an APPROVE, and each pin earned by a mutation the implementer reported with numbers — of which I reproduced the one it flagged as hardest. An opus re-review here would cost a full context to re-run mutations I have already run. Cost if wrong: a pin that does not bite, which cannot break production because no production line moved.
Task 10: GATES GREEN at bc7d66d, rounds 2/3/4 all gated at once (they had been ungated since f7ab47e). lib 1542 passed / 0 failed / 3 ignored. cargo test --tests: every target 0 failed. npm 1383 in 126 files, 0 failed. ps clean before, between and after — no stray perf-check this time.
Task 10: PERF PAIR — branch 273.5 / 278.7 / 296.7 us (spread 8.5%) against base 6fe1112 at 503.2 / 499.4 / 502.0 us (spread 0.8%), same sitting, perf run BEFORE the suites as the brief asked. The branch reads FASTER than the base by 45%. Ruling: PASS, no re-measure. The direction is safe by construction — a regression cannot hide under a base that measured SLOWER — and the branch's 273 us against a budget of 525 passes on its own terms whatever the base did. The base side is the odd one: 503 sits above the 224-445 band this gate has produced for unchanged code all branch long, and the base worktree was freshly created, so its first run compiled plugin_load_profile. Not chased. This is now the FOURTH distinct way this gate has misled or surprised someone on this branch, and it reinforces the standing rule rather than denting it: only same-sitting pairs mean anything, and only the direction is readable.
Task 10: GATE 6 HAS NO BASE SIDE AND MY BRIEF WAS WRONG TO ASK FOR ONE. idle_players_block_cost is a test this branch ADDED, so at 6fe1112 it does not exist (0 passed, 1409 filtered out). Branch min 2.01 us / median 2.04 / p95 2.93. Comparable only against this branch's own earlier sittings — 1.89 at bf0cb58, 2.27 at round 4's, 3.26 at task 10's — i.e. unmoved within a spread that does not travel between sittings. Every future gate-runner brief must ask for the branch figure ALONE and name the earlier sittings for context.
Task 10: CLIPPY WAS +1 ON THE LIB TARGET AND IT WAS REAL — 68 branch vs 67 base, and the carried "identical 68/68" was measured against 3dabad0, a commit ON this branch, not against the merge-base. I diffed the two warning sets by (message, file, line) myself: the one addition is derivable_impls on PlayerSource's manual Default at audio/player.rs:44, added by task 4. Fixed by deriving Default with #[default] on the None variant, exactly as TriggerMode two declarations down already does (commit 49b6b4b). lib back to 67, matching the base. LESSON: a per-target clippy figure is only a baseline if the base side is the MERGE-BASE; a mid-branch commit smuggles the branch's own drift into the baseline.
Task 10: COMPLETE — commits f7ab47e..49b6b4b, four fix rounds, review clean, gates green.
Task 11: implementer DONE — commit 0837972, and it changed NO PRODUCTION CODE AT ALL: 81 insertions, every one inside mod tests. Its claim was that player_fire/player_stop already implement all three modes (task 9 passed `looping` through; task 9 fix round 1 pinned Loop and stop-all), so the gap was coverage plus two fixtures. Four deviations from the brief, each argued. lib 1545 / 0 / 3.
Task 11: review (opus, diff 49b6b4b..c201dd2) — NEEDS FIXES, test-level, and THE PRODUCTION CLAIM HOLDS. Verified in the code rather than from the report: player_fire reads p.trigger.mode off the LIVE session every press (mod.rs:2029) and maps only == Loop to the clock's looping flag; ClockTable::advance (clock.rs:561-588) genuinely ends a non-looping clock at its end and wraps a looping one, so one-shot self-termination is IN THE CLOCK and not asserted into existence; and task 3's document-only ruling holds — session.rs:2333 writes p.trigger.mode, effect.rebuild = matches!(path, Raw | PlayerSource), so TriggerMode triggers no rebuild and player_clocks survives the commit. All three of the implementer's mutation REDs reproduced exactly, with its own assertion messages.
Task 11: THE FAILURE MODE HERE IS THE OPPOSITE OF THE BRANCH'S USUAL ONE. Not a test that re-implements its logic — none of the three do — but UNDER-ASSERTION: three tests pass against a mutation that breaks the very thing the test is named for. The reviewer proved each by mutation rather than by reading. (1) Making a retrigger a COMPLETE NO-OP (early return when the clock is already on) leaves all 126 control::tests green — the test asserts only that b is on and never looks at a, so V-4's headline property, that a retrigger rewinds THIS player, is entirely unpinned. (2) Firing non-looping clocks with len/8 instead of len leaves the one-shot test green; it advances exactly 48_000 and asserts off, so it cannot tell "ends at 48000" from "ends at 6000", and there is NO audio-side test pinning the fired length at all. (3) The gate test passes verbatim with OneShot, or with the set_trigger_mode line deleted.
Task 11: Ruling: GATE AND ONE-SHOT ARE IDENTICAL IN THE ENGINE BY DESIGN, AND THAT STAYS. The design's gate is "sounds while held; release cuts it" — the release is a pointerup, so the whole difference is WHO CALLS player_stop, and the engine has no business knowing which. Production reads exactly one thing, == TriggerMode::Loop. What is wrong is only that nothing RECORDS it: the statement goes in production at player_fire's == Loop, where a reader hits it, and the gate test must be named for what it actually pins or dropped as redundant (two_players_sound_at_once_on_their_own_clocks already pins player_stop). Cost if wrong: a later reader adds a Gate arm to the engine that duplicates a frontend responsibility.
Task 11: Ruling: THE TRIGGER-MODE COMMAND SEAM IS TASK 11's, and this is a REAL SCOPE GAP the reviewer found by asking a question I put to it. There is NO generic Op::Set pipeline from the frontend — every mutation crosses via a bespoke #[tauri::command], 24 of them in control/mod.rs, and the player commands are players_get/player_add/player_remove/player_fire/player_stop only. Task 13's brief only READS trigger.mode; tasks 12, 14 and 15 never mention it. As the plan stood, V2 WOULD HAVE SHIPPED WITH LOOP AND GATE UNREACHABLE BY ANY USER, every player permanently at the OneShot default — and nothing in the plan would have noticed. The implementer is not at fault: the brief's own "Produces: no new public names" is what reclassified set_trigger_mode as test-only, and that line reads as scoping THIS task's ControlPlane API, not as licensing the command layer to be skipped by everyone. A task called "Trigger modes" that leaves every mode but the default unreachable has not delivered trigger modes. The pad inspector that exposes it is V-12's and stays task 13's.
Task 11: MY DISPATCHES HAVE BEEN GIVING THE WRONG DESIGN-DOC PATH ALL BRANCH LONG — docs/specs/2026-08-26-plan-v-players-design.md does not exist; the file is at docs/superpowers/specs/. PROGRESS's own relative link is correct, so agents that followed the link were fine and agents that took my path string had to find it themselves. Corrected in task 11's fix brief and in every dispatch from here.
Task 11: fix round 1 dispatched (resume implementer, sonnet per the model policy — a missing command is a LOUD failure, not a silent one). Five items: three under-assertions each earned against the reviewer's own mutation, the Gate statement in production, and the command seam.
Task 11: fix round 1 implementer DONE — commit 7ee1abd, all five items. Command seam added: ControlPlane::set_trigger_mode + #[tauri::command] player_set_trigger_mode, registered at lib.rs:331 beside the other four player commands; the test-only free function deleted in favour of calling it. Gate test DROPPED as redundant and replaced with set_trigger_mode_reaches_the_document; the Gate/OneShot-are-identical statement moved into production at player_fire's == Loop. lib 1545 / 0 / 3.
Task 11: fix round 1 re-review (opus, diff c201dd2..7ee1abd) — all five items ADDRESSED, all three mutation REDs reproduced verbatim, production change sound. Two Important, both one assertion. It also answered the four questions I put to it: (1) the seam IS complete — commit runs with HistoryMode::Record and the Op::Set{ObjectRef::Player} arm builds its inverse from STORE TRUTH (session.rs:954, `read_player_prop` explicitly commented "truth, not caller's from"), so from: Value::Null is RIGHT and is the crate-wide convention for an advisory `from`, documented in those words at mod.rs:3006 — not a latent undo bug; (2) cp.players() clones the in-memory store with no deserialize, so the READ never meets serde and the round trip is symmetric; (3) nothing was lost dropping the gate test — two_players_sound_at_once_on_their_own_clocks pins both halves of player_stop; (4) p.trigger.mode is read in exactly three places crate-wide, two of them generic read/write plumbing, so the production comment is behaviourally true.
Task 11: THE UNDER-ASSERTION SURVIVED THE ROUND CONVENED TO REMOVE IT, in the very test carrying the branch's headline property. retriggering_one_player_leaves_another_sounding checked b with is_on ONLY. The reviewer replaced player_fire's single fire with a loop firing EVERY entry in player_clocks — one pad press rewinds all pads, which is EXACTLY the single-overlay behaviour V-4 deleted — and all 126 control::tests stayed green. An on/off flag cannot see it; only a playhead can. Both halves of that test needed a position, not a flag.
Task 11: THE SEAM'S WIRE FORM WAS UNPINNED FOR THE TWO MODES IT EXISTS FOR. TriggerMode's Rust round trip is SYMMETRIC, so it cannot see a rename: #[serde(rename = "GATE_XX")] on Gate left audio::player::tests AND control::tests fully green. player.rs:149 pinned only the default's "oneShot". Task 13's TS caller sending mode: "gate" would have been rejected by Tauri's arg deserialization at runtime, with a green Rust suite. This is the general shape worth remembering: a symmetric serialize/deserialize test pins NOTHING about the wire contract with a non-Rust caller.
Task 11: Ruling: I FIXED BOTH MYSELF rather than opening fix round 2 with an agent (commit 09c72b2). Each is one assertion whose mutation the reviewer had already named and run, so a round trip through implementer + re-review would have cost two contexts to re-derive what was already written down. Earned both by hand: the fire-every-clock mutation now fails on b's playhead (left 0, right 128) where it left 126 green before, and the GATE_XX rename now fails on the wire name (left String("GATE_XX"), right "gate"). Reverted both; control::tests + audio::player 131/131 green.
Task 11: Minor NOT FIXED, both recorded — (a) the production comment's "the one and only place trigger mode does anything at all" is behaviourally true but a reader who greps lands on session.rs's generic plumbing and briefly doubts it; (b) set_trigger_mode returns () where the neighbouring property setters return the mutated object, so task 13's caller must follow every write with a players_get. Style, consistent with player_fire/player_stop. To the final review.
Task 12: implementer DONE — commit 4b4292d, 748 insertions across midi/launch.rs, control/mod.rs, control/session.rs, control/snapshot.rs and a new tests/player_migration.rs. Five deviations, each argued. lib 1553 / 0 / 3; player_migration + v3_migration 10 passed.
Task 12: review (opus, diff 09c72b2..4b4292d) — REQUEST CHANGES, 2 CRITICAL and 3 Important, and this is the review that justifies the whole process on this branch. It reproduced EVERY mutation the report claimed and found the migration function itself careful and well-tested. The findings are entirely in what the report did not look at. Deviations 1 (wiring site — audio/project.rs has no open_project at all, only two doc-comment references; the brief's file list was simply wrong), 2 (tracks parameter) and 3 (dangling Clip preserved) judged CORRECT; deviation 3 better argued by the reviewer than by the implementer. Deviation 5 (in-memory only) CORRECT-BUT-UNDER-ARGUED. Deviation 4 WRONG.
Task 12: CRITICAL 1 — `session.midi.loaded_dir = None` LEAKS THE PREVIOUS PROJECT'S MIDI INTO THE NEXT ONE. adopt_midi_from_dir (midi/mod.rs:213-262) has THREE branches and the implementer reasoned about two. The Ok(None) branch is guarded by `if midi.loaded_dir.is_some()`, and THAT guard is what clears the previous project's clips, launch_maps and harmony when the newly-opened project has no v2+ midi document. Forcing loaded_dir = None makes it permanently false. Ok(None) is not the exotic legacy case it looks like: midi::persist shares project.json with the audio project (persist.rs:38) and load_from_project returns Ok(None) for schemaVersion < 1 or absent (persist.rs:345-348), so EVERY project that has never had a midi save takes it. Open A then B: B shows A's clips, A's harmony, A's bindings; the migration mints players from A's bindings into B's store.players; the next edit persists all of it into B's project.json. Verified by the reviewer with a scratch integration test, and commenting out the line makes that test pass while making the two-open test fail (left 0, right 1) — so the line is unambiguously the cause, and it is the SAME LINE the implementer's own RED exercised without noticing the other branch. THE BUG IT WAS CHASING IS REAL AND WELL-DIAGNOSED; only the fix is wrong. Three narrower parts of deviation 4 checked out and stand: nobody else relies on the same-dir skip (plugins/state.rs:1902 is a test helper), the dirty guard is intact (the skip is checked first, dirty second), and a second open's cost is acceptable.
Task 12: Ruling: FIX CRITICAL 1 AT THE RIGHT LEVEL — an explicit force-reload argument to adopt_midi_from_dir so an explicit open bypasses the same-dir cache hit WITHOUT disabling the Ok(None) reset, or run the migration only when adopt actually adopted. Implementer's choice, argued in the report, with a test for the A-then-B case as well as the two-open one. Cost if wrong: the corruption above, on a user's saved project, at open, silently.
Task 12: CRITICAL 2 — THE FRONTEND HAS NO `player` VARIANT AND THE PANEL THROWS ON EVERY MIGRATED BINDING. ipc.ts:93-95 is region|clip; the Rust wire form (correctly pinned at launch.rs:1240-1255) is {"kind":"player","playerId":...}. The union is only ever NARROWED, never exhaustively switched, so tsc stays green. LaunchMapPanel.svelte:32-42's targetLabel falls through to b.target.trackIds.map(...) — undefined for a player target, TypeError during render — called per row at :320, and launch.svelte.ts:105 re-pulls the snapshot on project://changed, which open_project_epoch emits. So it fires on the very open that runs the migration. The user opens a project that ever had a clip pad and the LAUNCH panel breaks; every binding they own becomes unreachable and the only rows that still render are the DANGLING ones — the exact inversion of the intent. Three more silent ones on the same seam: launch-map.ts:143/184 treat "not region" as clip so click-to-jump and the timeline overlay die for every migrated pad, and launch.svelte.ts:292's mapClip stops matching so "map this clip to a pad" CREATES A SECOND BINDING on a new note for a clip that already has one, with :302's self-trigger warning silently gone.
Task 12: Ruling: THE FRONTEND LaunchTarget SEAM IS TASK 12's — SAME REASONING THAT PUT TASK 11's COMMAND SEAM INTO TASK 11, AND THIS IS THE SECOND TIME. The reviewer read every remaining brief: task 13 is the CONTROL SURFACE (control-surface.ts's SurfaceTarget, SurfacePanel.svelte, Rack.svelte), a different type in different files; task 14 is the perf gate; task 15 is docs. grep over the briefs for LaunchTarget/LaunchMapPanel/launch-map hits only task 8 and task 12. Nothing after this task owns ipc.ts's LaunchTarget, launch-map.ts, LaunchMapPanel.svelte or launch.svelte.ts — which is what makes Critical 2 PERMANENT rather than transitional. A migration that makes every saved launch binding unrenderable has not migrated anything. LESSON, now twice paid: when a task adds a variant or a property, ASK WHO OWNS EVERY CONSUMER OF IT before closing the task, and check the remaining briefs by grep rather than by assumption.
Task 12: IMPORTANT 3 — the exhaustiveness sweep missed launch.rs:714, the drive path's self-trigger guard, a non-exhaustive `if let LaunchTarget::Clip` that stops a drive clip from firing the pad that plays that very clip. After migration the target is Player, the guard never matches, and the clip re-fires its own player on every drive note-on: the material sounds twice and restarts, with the frontend's matching warning silenced by Critical 2 at the same time. Full count: SEVEN Rust sites and six frontend. The other five Rust sites are correct, including snapshot.rs:580 (the right line, and sufficient — the snapshot carries launch_maps and players wholesale and republish_full runs after the migration in the same lock).
Task 12: IMPORTANT 4 — THIRD TASK IN A ROW TO SHIP AN UNDER-ASSERTION. The reviewer replaced launch.rs:198's target assignment with `players[0].id.clone()` — always the first player — and ALL 41 midi::launch unit tests and ALL 6 player_migration integration tests passed. No fixture in either file has two distinct clips, so players[0] is trivially right everywhere. The unguarded failure is precisely the one this task fears: two pads on two different clips, and pad 2 plays clip 1's material. migrate_reuses_an_existing_player_already_on_the_same_clip asserts an id rather than a count, which is the right instinct, but with one player in the vec it cannot discriminate.
Task 12: question 1 ANSWERED — bypassing the transaction channel is RIGHT for an open-time migration. It is not journaled, not undoable and does not mark dirty, and epoch_boundary clears both history stacks immediately after; republish_full() runs inside the same lock after the migration and the snapshot carries both the new players and the retargeted bindings. Minor coverage note: no test in the diff exercises the PUBLISHED snapshot path the frontend consumes — cp.players() and cp.launch_snapshot() both read the live session. Confirmed sufficient by reading, so a note, not a defect.
Task 12: CARRIED TO TASK 13 — PlayerId is not stable across a close/reopen of an unedited project, and after Critical 1's proper fix that churn is guaranteed on every re-open. Nothing outside the document persists a PlayerId today, so nothing breaks yet, but TASK 13 IS ABOUT TO PERSIST playerId INTO SURFACE LAYOUTS. That interaction must be settled before task 13 stores an id.
Task 12: fix round 1 dispatched (resume implementer, sonnet). Vitest scoped to the touched frontend files is explicitly allowed this round; the full npm suite is still the gate-runner's.
Task 12: SESSION LIMIT KILLED THE FIX-ROUND-1 IMPLEMENTER MID-RUN — second implementer lost to a limit on this branch, after task 9's. Controller took custody of its tree as 36004ba, labelled UNVERIFIED, per the d1f8cdd precedent. `cargo check --lib` clean is the only thing known. KNOWN-INCOMPLETE and worth more than the compile fact: the diff touches ONLY Rust (control/mod.rs, midi/launch.rs, midi/mod.rs, plugins/state.rs, tests/player_migration.rs) and not one of ipc.ts, launch-map.ts, LaunchMapPanel.svelte or launch.svelte.ts — so the entire frontend half of Critical 2 is still owed, which is the half that breaks the launch panel on every migrated binding.
Task 12: fix round 1 RE-dispatched to a FRESH implementer (sonnet, per the model policy). Told explicitly that 36004ba is unreviewed and that its job STARTS with reading that diff critically against the brief, finding by finding, and reporting which findings it addressed, which it addressed badly, and which it never touched. Scoped vitest explicitly allowed this round because the round changes a type union and four consumers; the full npm suite stays the gate-runner's.
Task 12: NOTE FOR WHOEVER SYNCS THE LEDGER — the git-ignored working copy at .superpowers/sdd/2026-08-28-plan-v2-players/progress.md is STALE, ending at task 10's fix round 3. The successor session wrote to the COMMITTED ledger instead, which is the current one. Do not copy the working file over the committed one; that would delete tasks 10 (rounds 3-4), 11 and 12 wholesale.
Task 12: THE CUSTODY PROTOCOL JUST PAID FOR ITSELF. The fresh implementer found a MUTATION-TEST ARTIFACT left in PRODUCTION code in the inherited tree 36004ba — launch.rs's self-trigger guard had `LaunchTarget::Player { .. } => false, // MUTATED: player self-trigger guard removed`. That line IS Important 3's defect, sitting in the tree wearing the fix's clothes: the dead implementer had mutated it to prove its test bites and was killed before reverting. Controller verified this by hand (`git show 36004ba -- src-tauri/src/midi/launch.rs`, line 52 of the hunk). Had anyone trusted the custody commit because "cargo check --lib is clean", the branch would have shipped Important 3 UNFIXED with a test suite that looked green and a diff that looked like a fix. Verified the current tree carries no such marker: grep for MUTATED/MUTATION over src-tauri/src and src returns one hit, a legitimate comment in a pre-existing hum.rs test. LESSON: a killed implementer's tree is not half-finished work, it is work in an ARBITRARY intermediate state, and a mutation probe is the most dangerous state to be caught in.
Task 12: UNEXPLAINED — the fresh implementer reports the working tree changed under it mid-session more than once with no commit boundary it could account for, "code matching my intended fixes appeared before I wrote it in a few spots". ListAgents showed NO other Claude session on this machine at dispatch time, and the tree is clean now at b86a4b5. Most plausible reading is the killed agent's process still flushing writes, but that is a guess and it is recorded as one. CONSEQUENCE FOR THE REVIEW, not optional: the reviewer must verify the COMMITTED TREE independently and may not accept the implementer's report as evidence for any line. That is the standard anyway; here it is load-bearing.
Task 12: Ruling: THE SCOPE GROWTH IS ACCEPTED, and the implementer was right to grep rather than stop at the brief's four named files. The seam is defined by THE BUG, not by the filename: any consumer of LaunchTarget that breaks for a player target belongs to task 12, because task 12 is what makes the target a player. So surface.svelte.ts's isClipPlaying/fireClip (same kind === "clip" bug, breaks the pad-grid lit indicator for migrated pads) is task 12's, and it does NOT collide with task 13, which owns control-surface.ts's SurfaceTarget — a different type in different files. tauri.ts's playersGet wrapper is accepted on necessity: task 9 shipped the players_get command with no frontend wrapper, and without one mapClip/clipSelfTriggers are provably unfixable. Cost if wrong: task 13's brief finds two of its files already touched, which is cheaper than a migration that leaves the pad grid lying about which pads are lit.
Task 12: fix round 1 re-review (opus, diff 4b4292d..b86a4b5) — ALL FOUR FINDINGS ADDRESSED, and Important 4 is EARNED THREE TIMES OVER. The reviewer re-ran its own players[0] mutation against the new fixtures and named exactly what makes each pin bite: migrate_assigns_each_binding_the_player_for_its_own_clip uses two distinct clips on two distinct tracks with distinct instrument ids, so PlayerSource — not a count — discriminates; migrate_reuses_an_existing_player_already_on_the_same_clip now places a DECOY player first in the vec, so players[0] resolves to the decoy; and the integration test covers the same shape through real open_project_epoch + cp.players()/cp.launch_snapshot(), which also closes the published-snapshot coverage gap the brief flagged. First task on this branch where the reviewer's mutation is genuinely earned rather than survived. Critical 1's A-then-B test verified at the premise level, not taken on trust: project.rs:50 writes schema_version 1 and persist.rs:345-347 returns Ok(None) for version < 2, so B genuinely takes the reset branch, and the discrimination lives in the SECOND assertion because cp.players() is empty either way.
Task 12: THE SELF-TRIGGER GUARD IS INDEPENDENTLY CONFIRMED CORRECT, not merely un-commented — launch.rs:393-409 is an exhaustive match whose Player arm resolves the id through PlayerSource::MidiClip's clip_id, pinned positive, different-clip negative, Region negative and pre-migration Clip. That was the specific thing the mutation artifact could have hidden.
Task 12: NEW IMPORTANT INTRODUCED BY THE ROUND — surface.svelte.ts:380-386. fireClip's old body pre-checked for an existing binding and only called mapClip when there was none. The new body delegates UNCONDITIONALLY to mapClip, whose existing-binding branch sets selectedId and calls focus(), which ends in view.scrollToSamples() and void transport.seek(). So pressing a pad now scrolls the timeline and SEEKS THE PLAYHEAD — during playback it relocates the transport. It contradicts preview's own contract at launch.svelte.ts:283 ("loop and playhead stay put"), which is exactly why fireClip avoided mapClip on the hit path. The report called it behaviour-neutral by reasoning only about the dedup and missing the focus side effect. Nothing pins it.
Task 12: Ruling: FIX IT BY SEPARATING THE TWO JOBS, not by either shape the reviewer offered. mapClip does two things — find-or-create a binding, and focus/select it, which is UI intent belonging to the "map this clip to a pad" gesture. fireClip wants only the first. A player-aware pre-check in fireClip would duplicate the matching logic mapClip already owns, and duplicated matching is exactly the drift Critical 2 came from; a no-focus flag argument would put a bool on a public method to select which half of it runs. Extract the find-half as a player-aware bindingForClip(clipId) and have BOTH call it: mapClip = bindingForClip ?? create, then focus; fireClip = bindingForClip ?? mapClip. One matching implementation, no flag, no duplication. Cost if wrong: one more small method on the store.
Task 12: Ruling: THE MINOR IS PROMOTED INTO THE ROUND. launch.svelte.ts:239's un-awaited reloadPlayers would normally be deferred, but its failure mode is not cosmetic: a pad press landing inside that one-IPC-round-trip window takes mapClip's CREATE path and mints a duplicate binding — the very bug Critical 2 exists to close. The doc comment also asserts an ordering the code does not provide. `await` costs nothing here.
Task 12: fix round 2/5 dispatched (resume the fresh implementer, sonnet).
Task 12: THE "UNEXPLAINED WORKING TREE" IS EXPLAINED, AND IT IS THE CONTROLLER'S DOING. I committed my ledger updates with `git add -A` while an implementer was live in the same worktree. That swept the implementer's in-flight source edits into commits whose messages say "docs: ledger". Proven: d63ff1e ("docs: ledger — custody of task 12's half-done fix round") carries 9 lines of src-tauri/src/midi/launch.rs, and 8f2e7e7 ("docs: ledger — round 1's findings closed") carries the ENTIRE fireClip fix — src/lib/state/surface.svelte.ts, launch.svelte.ts and a new surface-panel.dom.test.ts, 52 lines. `git log -S existingBindingForClip` names 8f2e7e7 as the origin of the fix, which is why two successive implementers reported "code matching my intended fixes appeared before I wrote it, with real commits attached". They were not imagining it and they were right to flag it; the ghost was me.
Task 12: Ruling: NO HISTORY REWRITE, but the practice stops now. NEVER `git add -A` while an implementer is live in this worktree — stage explicit doc paths only. Rewriting the two commits would mean rewriting pushed history to fix a message, and the code itself has not escaped review: d63ff1e landed BEFORE b86a4b5, so round 1's review (4b4292d..b86a4b5) covered it, and 8f2e7e7 lands after, so round 2's re-review (base b86a4b5) covers it. That is luck of the ranges rather than design, which is exactly why the practice stops. Cost of leaving it: `git log --oneline` and any bisect misattribute the fireClip fix to a docs commit; recorded here so the next reader is not misled.
Task 12: fix round 2 implementer DONE — commit b27b0eb (plus the sweep in 8f2e7e7). Item 1 fixed as ruled: existingBindingForClip is the side-effect-free lookup, mapClip runs only to CREATE, and fireClip calls the former first. Item 2's ordering awaited. 43 midi::launch + 8 player_migration green, tsc clean, 74 vitest across touched files.
Task 12: THE IMPLEMENTER CAUGHT ITS OWN TEST BEING VACUOUS, TWICE, AND SAID SO. Its first fireClip pin asserted launch.selectedId stayed null — but preview() has ALWAYS set selectedId on every fire, predating the branch, so the assertion tested a premise that was never true rather than the regression. And its reload()-ordering test resolved playersGet manually before awaiting reload(), so the fire-and-forget mutation got a microtask turn and passed regardless; replaced with a setTimeout so playersGet resolves on a strictly later tick, then verified RED against `void this.reloadPlayers()`. On a branch whose signature failure is a test that passes while production is broken, an implementer that audits its own green tests for vacuity is the behaviour to keep.
Task 12: CORRECTION TO THE ENTRY ABOVE — "the controller" IN IT IS NOT THE SESSION CONTROLLER, AND A STRANGER WOULD READ IT THAT WAY. The entry was written in the first person by task 12's implementer, which had spawned sub-agents of its own and was acting as their controller; the `git add -A` was ITS, not the session controller's. Evidence, by timestamp: the session controller's last commit before the fix round was 87a2282 at 21:16:42, and it touched nothing in the worktree afterwards; every commit from 36004ba at 21:22:17 onward — including both mislabelled ones, d63ff1e at 21:23:08 and 8f2e7e7 at 21:48:40 — was made by the implementer. The session controller's ledger commits this whole branch have used `git add -A docs/`, scoped. Left uncorrected, this entry teaches the wrong lesson to the next session: it would warn a controller off a practice it does not have, and say nothing about the one that actually happened.
Task 12: THE REAL LESSON, RESTATED — AN AGENT THAT SPAWNS SUB-AGENTS INHERITS THE WORKTREE RULE ALONG WITH THE ROLE. CLAUDE.md's worktree warning is about two workers in one tree, and it does not care which of them is a controller. Whoever holds the tree while someone else edits it must stage explicit paths. The mislabelling stands (no history rewrite: d63ff1e landed before b86a4b5 so round 1's review range covered it, and 8f2e7e7 falls inside round 2's) but `git log --oneline` and any bisect now misattribute the fireClip fix to a docs commit, and that is the cost of finding this late rather than never.
